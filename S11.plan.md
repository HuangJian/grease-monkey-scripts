# S11 — §4/§5 剩余项全收尾（用户选「全部」）

> 设计先行：本文件待 review，实施前不改代码。S10 已提交 `0e7f8f4`（§5 工程化收尾）。本阶段按用户在 S11 范围选择「全部」，一次性覆盖 `frontend.refactor.md` 中 S10 之后仍开放的 **§4.8 / §4.11 / §4.13 / §4.10 / §5.6** 五项。阶段较大，拆为 5 个子任务，每个可独立验证。

## 0. 范围来源

S10 之后 `frontend.refactor.md` 仍开放条目：

- §4.8 抓取层 7 份重复的 request 包装 + status/超时不一致
- §4.11 其余重复实现（prune / time-parse / URL 重写）
- §4.13 兼容性与细节
- §4.10 GBK 编码零兜底（需实测）
- §5.6 测试覆盖缺口

## 1. 现状核对（已查证，非假设）

### §4.8 request 收敛

- `shared/request.ts` 已有 `requestText`(:108) / `requestJson`(:117) / `requestTextWithHeaders`(:63)；`weather` / `v2ex` / `novels` 已走 shared。
- 仍**直接** `runtime.request`：`tnews/fetcher.ts:10`、`reddit/fetcher.ts:40`、`hupu/fetcher.ts:22`、`misc/openrouter/fetcher.ts:26`、`xueqiu/ai/summarize.ts:101`（共 5 处）。
- status 校验分裂：`>=400` 拦截仅 tnews/reddit/hupu/misc 有；v2ex/novels/weather/xueqiu 无。超时分裂 15s/20s。

### §4.11 重复实现

- `shared/prune.ts` 已存在（:20「shared core of the four `pruneExpiredCache` implementations」），但 **4 份 feature wrapper 仍在**：`xueqiu/source.tsx:216`、`v2ex/source.tsx:56`、`hupu/source.tsx:56`、`reddit/source.tsx:58`（各 `async function pruneExpiredCache(runtime)` 本地副本）。
- `shared/date-parse.ts` 已存在；`time-parse` 是否仍重复待 impl 精确核对（hupu/parser、novels/adapters/sudugu、weather/cma/parse-page）。
- `novels/fetcher.ts:330-335` 有 `baseUrl` + `rewriteHost(baseUrl, host)`；与 `novels/mirror.ts` 是否重复待核。

### §4.13 兼容性

- `xit/render/list.tsx:29` `TAG_RE = /(?<=\s|^)#(...)/g` **lookbehind 仍在** → Safari < 16.4 `SyntaxError` 解析崩溃。
- `app/index.ts:183` `runtime.addValueChangeListener(...)` 返回值**被丢弃**，`Dashboard` 无 `destroy()` / `removeValueChangeListener`。
- `reddit/scoring.ts:45` `subFromUrl(url)` 归属校验；`hupu/scoring.ts` **无 board 校验**。
- `shared/expandable-list.tsx` 现仅 `CSS.escape`(:24)，**`escapeHtml` 不一致项已不存在** → 本计划跳过。

### §4.10 GBK

- `src/prism` 下 grep `charset|TextDecoder|gbk` 零命中；`novels/fetcher.ts` 直接取 `response.responseText`。sudugu/deqixs 为中文小说站，若按 UTF-8 解码 gbk 页面 → 乱码 → `chapterNumber()` 失败 → 跨站合并/已读失效（连锁）。
- 需 `curl -sI` 实测确认实际 `Content-Type`。

### §5.6 测试缺口

- 零测试模块：`app/source-registry.ts`、`app/lifecycle.ts`、`app/sync-bootstrap.ts`、`app/shortcut-bootstrap.ts`、`shell/editor.tsx`、`header-state.ts`、`xit/component/header.tsx`、`xit/editor.tsx`、`misc/**`。
- 优先补纯逻辑单测（锁定本次修的 bug）：`sanitize` / `header-state` / `source-registry` / `merge`。

## 2. 子任务

### S11.1 §4.8 抓取层 request 统一

- **目标**：tnews/reddit/hupu/misc/openrouter/xueqiu-ai 的 `runtime.request` 收敛到 `shared/request`（`requestText`/`requestJson`），统一 `status>=400 抛错` + 默认 `15s` 超时。
- **做法**：逐 fetcher 比对语义（header、响应解析、既有 status 处理），改为调用 shared；保留各 fetcher 特有解析。xueqiu/ai/summarize 同样收敛。
- **风险**：中。需比对 5 处语义差异，避免改坏；统一 status/超时会改变既有失败路径 → 必须定向测试兜底。
- **验证**：各 fetcher 定向测试 + typecheck/lint。

### S11.2 §4.11 重复实现抽取

- **目标**：4 份 `pruneExpiredCache` feature wrapper → `shared/prune.ts` 单一导出；核 `date-parse` / `novels` URL 重写是否仍重复并合并。
- **做法**：`shared/prune.ts` 导出 `pruneExpiredCache(runtime)`，4 fetcher 删本地 wrapper 改 import；`novels/fetcher.ts` 的 `rewriteHost` 与 `mirror.ts` 重复则合并进 `mirror.ts`。
- **风险**：低-中。wrapper 语义需一致（现有注释提及 fetch 失败时的状态保护逻辑，须保留）。
- **验证**：定向测试 + typecheck/lint。

### S11.3 §4.13 兼容性

- **目标**：
  1. `xit/render/list.tsx:29` lookbehind `(?<=\s|^)` → `(?:^|\s)` + 捕获组（修 Safari < 16.4 解析崩溃）。
  2. `app/index.ts:183` 捕获 `addValueChangeListener` 返回值，补 `Dashboard.destroy()` / `removeValueChangeListener` 清理。
  3. `hupu/scoring.ts` 补 `boardFromUrl`（对齐 reddit `subFromUrl`）。
- **非目标**：`expandable-list.tsx` escapeHtml 不一致（已不存在，跳过）。
- **风险**：低。
- **验证**：list 渲染定向测试 + typecheck/lint。

### S11.4 §4.10 GBK 编码兜底

- **目标**：`curl -sI` 确认 sudugu/deqixs 实际 `Content-Type`；若声明 `gbk`/`gb2312` 而按 UTF-8 解码 → 给 `NovelAdapter` 加 `charset?: string` 兜底（`runtime.request` `responseType:'arraybuffer'` / `overrideMimeType`）。
- **风险**：外部依赖（需网络实测）；**可能实测后无代码改动**（若站点已是 UTF-8）。
- **验证**：记录 curl 结果；若改，对应定向测试。

### S11.5 §5.6 测试覆盖缺口

- **目标**：补 `sanitize` / `header-state` / `source-registry` / `merge` 纯逻辑单测（优先，锁定本次修的 bug）；可视成本补 `sync-bootstrap` / `shortcut-bootstrap` / `shell/editor` 等低成本项。
- **风险**：低。
- **验证**：`bun test` 相关文件 pass；整体测试数 ≥ 改前。

## 3. 整体验证

- `typecheck` / `lint` / `format` 全绿；`lint` 27 `perf` warning 非阻塞（既有 smell，非本次引入）。
- 定向测试子集（**避免全量 `bun test` 在沙箱挂起**）：各 fetcher + prune + list 渲染 + scoring + 新增测试。
- `build` 全绿，报告 Build hash。
- 提交方式：按惯例整 S11 一个 commit（或按子任务拆 S11a/b，待用户定）；`S5.plan.md` 仍排除。

## 4. 风险与开放项

- 阶段较大（5 子任务）。若用户偏好拆分，可拆 S11a(§4.8/§4.11) + S11b(§4.13/§4.10/§5.6)。
- §4.10 依赖外网实测；§4.11 的 `time-parse` / `novels` URL 重复待 impl 精确核对（可能已部分完成）。
- §4.8 统一 status/超时可能改变失败语义 → 必须以定向测试兜底，不依赖全量 suite。
- lookbehind 修复需确认替换后正则在各浏览器（含旧 Safari）等价。
