# S6 工程化收尾

> 阶段：S6（路线图 `frontend.refactor.md:376`）
> 日期：2026-09-02
> 上游：S5 已提交 `9c193cb`（perf：消除三处性能悬崖）
> 门禁：`bun run check` 绿 + `Build hash` 上报

## 0. 范围与决策

路线图 S6 四项（5.1 / 5.2 / 5.5 / 5.6）。本轮**排除** §5.3（`console.debug` 仍入 prod，与 AGENTS.md 约定矛盾）与 §5.4（oxlint 规则面），原因：二者不在路线图 S6 列表内，避免扩大范围。

**§5.5 范围决策（已与用户确认）**：开启 `tsconfig.json` 两个严格 flag —— `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`，并修正产生的全部 **228** 处 typecheck 错误。

实测成本（开启前测量）：

- 仅 `noUncheckedIndexedAccess`：**178** 处（`src`≈52，`test`≈126）
- 叠加 `exactOptionalPropertyTypes`：**228** 处（约 +50，交互非完全加性）
- 故 228 主要来自 `exactOptionalPropertyTypes`，但用户选择全开以求最大类型安全。

## 1. 目标

1. ① dist 孤儿清理（5.1）：构建后删除无对应 `src/<name>/index.user.ts` 的 `dist/*.user.js` / `dist/*.debug.js`。
2. ② SWC 降激进（5.2）：关闭 `unsafe` / `properties` / `hoist_props`；移除 `void 0`→`0[0]` 字符串 hack。
3. ③ tsconfig 收紧（5.5）：开启两个严格 flag，修正 228 处错误。
4. ④ 补单测（5.6）：为 `header-state.ts` 加单测（sanitize 已在 S1、merge 已在 S5 覆盖）。

## 2. 非目标

- 不处理 §5.3（`console.debug` 入 prod）——保留现状，仅记录。
- 不处理 §5.4（oxlint `import/no-cycle`/`perf` 等）——保留现状。
- 不动 §5.5 的依赖冗余（`types:["bun-types"]` vs `@types/bun`、`jsdom`+`happy-dom` 并存、`typescript ^7`）：属独立重构，风险高、收益低，移出 S6。
- 不修改任何行为逻辑，仅工程化 / 类型安全。

## 3. 设计

### 3.1 ① dist 孤儿清理（5.1）

位置：`scripts/build-userscript.ts` `main()`（`:364` 起）。

当前 `main()` 仅依据 `src/*/index.user.ts` 发现 6 个活入口并构建；`dist/` 中 5 个孤儿（`a-dark-room-robot` / `drag-and-drop-link-opener` / `ga-rss-filter` / `geekpark-better-ui` / `lizhi-fm-web-ui`）从未被清理，且仍含老源码 `console.log`。

方案：在 Phase 2 之后（`:435` 之后、最终报告之前）增加 **purge 步骤**：

- 计算 `liveNames = new Set(entries.map((e) => basename(dirname(e))))`（即 6 个活名）。
- `readdir('dist')`，对每个 `*.user.js` / `*.debug.js`：取其基名（去扩展名）；若不在 `liveNames` → `unlink`。
- purge 始终执行（与 `--full` 无关），保证后续任何新增孤儿都会被自动清除。

不写独立 `scripts/clean-dist.ts`，直接在 `main()` 内联 purge（最小改动、与构建同源）。

### 3.2 ② SWC 降激进（5.2）

位置：`scripts/build-userscript.ts:108-134` `SWC_OPTIONS.compress`。

变更：

- 删除 `unsafe: true`（`:113`）——第三方页面下风险最高（假设 `Array.prototype` 未被篡改等）。
- 删除 `properties: true`（`:121`）——属性访问重写，minify-only bug 难定位。
- 删除 `hoist_props: true`（`:116`）——同上。
- 保留其余（`passes:3`、`drop_debugger`、`pure_funcs`、`toplevel`、`join_vars`、`reduce_vars`、`unused`、`dead_code`、`sequences`、`conditionals`、`comparisons`、`evaluate`、`booleans`、`loops`、`if_return`、`collapse_vars`、`inline:2`）——均为安全变换，且 `inline:2` 未被 §5.2 点名。

位置：`scripts/build-utils.ts:138-160` `postSwcOptimize`。

- 删除第 142-143 行 `void 0`→`0[0]` 替换及其注释（§5.2 点名的字符串级 hack）。
- 保留 ① JSX dev-arg 剥离、③ 简单 wrapper→arrow（非 hack，属合法瘦身）。

副作用：SWC 输出与 `build-utils.ts` 源码均变化 → 6 个活脚本的 content hash 变更 → `bun run build` 全量重建；5 个孤儿被 3.1 purge 删除。符合预期。

### 3.3 ③ tsconfig 收紧（5.5）

位置：`tsconfig.json:6`（`"strict": true` 之后）。

变更：

```jsonc
"strict": true,
"noUncheckedIndexedAccess": true,
"exactOptionalPropertyTypes": true,
```

修正 228 处错误，分类与手法：

- **`TS2532 Object is possibly 'undefined'` / `TS18048 'x' is possibly 'undefined'`**：下标访问 `arr[i]`、`.find()` 结果返回 `T | undefined`。
  - `test/` 中已知数据成立处：补 `!`（如 `arr.find(...)!`、`arr[i]!`）。
  - `src/` 中：改用 `arr[i] ?? fallback` / 提前 `if (!x) return` 守卫 / 收窄类型，禁止无依据 `!`。
- **`TS2488 Type 'T[] | undefined' must have Symbol.iterator`**：解构/for-of 作用于可能为 undefined 的数组 → 加 `?? []` 或守卫。
- **`TS2345 HTMLElement | undefined` 等参数类型不符**：调用前 `?? document.createElement(...)` 或断言。
- **`TS2769 No overload matches`**：多为 `exactOptionalPropertyTypes` 下向可选属性赋 `undefined`（`{ a?: number }` 不能写 `a: undefined`）→ 改为条件展开 `{ ...(cond ? { a: v } : {}) }` 或省略该键。
- **`exactOptionalPropertyTypes` 专属**：函数签名 `fn(x?: number)` 调用时传 `x: undefined` 报错 → 改为不传该参或 `fn(...(x === undefined ? [] : [x]))`。

执行方式：开启两个 flag 后反复 `tsc --noEmit` 抓取 `file:line`，逐文件修正；`src/` 优先用真实 null 处理，`test/` 用 `!`/guard。以 `bun run typecheck` 归零为完成判据。

### 3.4 ④ 补 header-state 单测（5.6）

位置：`src/prism/header-state.ts`（导出 `createHeaderState<S>`、`useHeaderState<S>`，无现测）。

新增 `test/prism/header-state.test.ts`：

- `createHeaderState`：
  - `get()` 初始值 == initial。
  - `set(updater)` 经 updater 更新并覆盖。
  - `subscribe(fn)` 在每次 `set` 后触发；返回 unsubscribe，调用后不再触发。
  - 多次 `set` 多次触发；unsubscribe 后仍可继续 `set`（不影响其它订阅者）。
- `useHeaderState`：用 `preact/hooks` + `happydom` 渲染（参考现有 `*.test.tsx` 的 hook 测试范式），断言组件随 `store.set` 重渲染；unmount 后取消订阅。

## 4. 文件改动清单

| 文件                              | 改动                                                                                                    |
| --------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `scripts/build-userscript.ts`     | `SWC_OPTIONS` 删 `unsafe`/`properties`/`hoist_props`（:113/121/116）；`main()` 增 dist purge（:435 后） |
| `scripts/build-utils.ts`          | `postSwcOptimize` 删 `void 0`→`0[0]`（:142-143）                                                        |
| `tsconfig.json`                   | 增 `noUncheckedIndexedAccess`/`exactOptionalPropertyTypes`（:6 后）                                     |
| `~40 个 src/test 文件`            | 修正 228 处严格类型错误（见 §3.3）                                                                      |
| `test/prism/header-state.test.ts` | 新增（§3.4）                                                                                            |

## 5. 测试与验证

1. `bun run typecheck` → 0 错误（§3.3 判据）。
2. `bun run test` → 全绿（header-state 新测 + 既有回归）。
3. `bun run lint` / `bun run format` → 干净。
4. `bun run build` → 仅 6 个活脚本产出；`dist/` 不再含 5 个孤儿（验证 3.1）。
5. `bun run check` 全绿；记录 `Build hash`。

## 6. 风险

- ③ 跨 ~40 文件、228 处类型修正，改动面大但 typecheck 门禁兜底；`src/` 处禁止无依据 `!`，避免掩盖真实 undefined 路径。
- ② SWC 输出变化使 dist hash 全变、全量重建——属预期，构建产物体积可能略增（去激进变换）。
- ① purge 误删风险：仅删 `dist/` 中基名不在 `liveNames` 的 `*.user.js`/`*.debug.js`，不影响活脚本；构建脚本本身只读 `src/`，purge 不会触碰源码。

## 7. 完成状态

（待实施）
