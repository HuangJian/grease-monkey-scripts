# S5 — 性能悬崖（Plan）

> 续 `frontend.refactor.md` §3（P2 性能），路线图 S5 行：「① novels `maxLatestWindow` 接线（3.1）；② merge O(n²)（3.2）；③ `mapLimit` 并发（3.3）；④ SafeLine PoW（3.4）」。
> 前置：S1–S4 已提交（S4 = `73d5311`）。本计划**只设计、不落地代码**，待审批后实施。
> S5 与 S4 正交：S4 已把 `Runtime` 收敛为定时器/时钟唯一边界，本计划的 SafeLine PoW 异步化将**复用 S4 的 `runtime.setTimeout`** 做主线程让出。

## 1. 目标

消除 `frontend.refactor.md` §3 列出的四处性能悬崖：

1. **§3.1 `maxLatestWindow` 完全未生效**：`fetchOneBook` 的 `options` 形参被命名为 `_options`（未使用），合并结果从不限长；尾页翻页 `for (p=2..lastPageNumber)` 全量串行抓取 + 每页 `...tailChapters.reverse()` 全量数组复制。
2. **§3.2 跨源合并 O(n²)**：`mergeSourceChapters` 在 spine/extras 两处对每章 `nodes.findIndex` 全量扫描；`textKey` 为空串（纯「第N章」标题）聚成大桶，桶内两两 `union` 最坏 O(k²)。
3. **§3.3 并发未收口（残余）**：S3 已加 `mapLimit` 且 `novels/fetcher.ts` 已接入，但 `app/refresh.ts:89` 仍用裸 `Promise.all(stale.map(...))`，冷启动无并发上限。
4. **§3.4 SafeLine PoW 主线程同步暴力破解**：`solveSafelinePow` 同步跑最多 1e6 次 SHA-1，难度上调即冻结主线程；超限返回 `'0'` 仍发一次注定失败的重试。

## 2. 非目标（明确排除）

- **§3.5 `hupu`/`tnews` 正则**：不在 S5（独立正则悬崖，可单列）。
- **`frontend.refactor.md:309` 折叠阈值 / `initialNewChapters` / `maxNewChaptersPerBook` UI 无效**：属于「配置项未接线」的同类问题但独立于 `maxLatestWindow`，不在 S5；列为后续 follow-up。
- **`crypto.subtle.digest` 长期替换**：§3.4 标注为「长期」，本计划用「异步让出 + 难度护栏」达成主线程不冻结，不引入 Web Crypto 依赖。
- **`config/defaults.ts` 其它默认值**：只动 `maxLatestWindow`（§3.1 点名）。
- **`mapLimit` 的 fail-fast 语义改变**：S5 仅把 `app/refresh.ts` 的 `Promise.all` 换成 `mapLimit`，失败快速语义不变（与现有 `Promise.all` 一致）。

## 3. 设计决策

### 3.1 `maxLatestWindow` 接线（锚点 `fetcher.ts:76,168` / `:221-232` / `config/defaults.ts:38`）

**问题定位**：

- `fetchOneBook(runtime, cfg, prevById, prevByUrl, _options, resolveAdapter)`（`:76`）—— `_options` 未使用，`mergeSourceChapters(inputs, seenKey)`（`:168`）从不限长。
- 尾页翻页 `fetchSourceChapters` 内 `for (let p = 2; p <= home.lastPageNumber; p++)`（`:221-232`）逐页串行；`:231` `chapters = [...tailChapters.reverse(), ...chapters]` 每页把已累积数组整体重建（O(pages²) 元素拷贝）。

**方案**：

1. `fetchOneBook` 形参 `_options` → `options`；取 `const maxLatestWindow = options.maxLatestWindow`。
2. `fetchSourceChapters` 增 `maxLatestWindow: number` 形参（`:115` 调用点补传），在其尾页循环内做两件事：
   - **首页已够则不翻页**：若 `home.homeChapters.length >= maxLatestWindow`，跳过尾页循环（home 已填满窗口）。
   - **翻页即收口**：用收集器 `const collected: RawChapter[][] = []`，每取一页 `collected.push(tailChapters)`；当 `home.homeChapters.length + collected 累计长度 >= maxLatestWindow` 时 `break`。循环结束一次性 `chapters = [...collected.reverse().flat(), ...homeChapters]`（**单次** `reverse().flat()`，消除逐页复制）。
3. `mergeSourceChapters(inputs, seenKey)`（`:168`）→ `mergeSourceChapters(inputs, seenKey, { maxWindow: maxLatestWindow })`。`MergeOptions` 增 `maxWindow?: number`（默认 `Infinity` = 不限）。
4. `config/defaults.ts:38` `maxLatestWindow: Number.POSITIVE_INFINITY` → `200`（按 §3.1 建议的有限默认）。**决策点**：这改变「未配置用户」的默认行为（从无限制变为 200 章窗口）。因该字段此前从未生效，启用即符合预期；若你倾向保持无限制默认，可保留 `Infinity`、仅接线（用户显式配置才生效）。

**窗口限长实现**（落在 `merge.ts`，纯函数、可单测）：
新增导出 `windowLatestChapters(chapters: NovelChapter[], seenKey: string, maxWindow: number): NovelChapter[]`，语义：

- `maxWindow === Infinity` 或 `chapters.length <= maxWindow` → 原样返回。
- `seenKey` 空 → `chapters.slice(0, maxWindow)`（输出为最新在前）。
- 否则：找 `seenIdx = chapters.findIndex(c => c.key === seenKey)`；若 `seenIdx < maxWindow` → `slice(0, maxWindow)`；若 `seenIdx >= maxWindow` → `[...slice(0, maxWindow), chapters[seenIdx]]`（保留已读边界章，置于末尾，与 `collapseChapters` 既有的边界章置尾一致）。
- `mergeSourceChapters` 在构建 `out`（最新在前全量）**之后、seen 切片之前**调用 `windowLatestChapters(out, seenKey, maxWindow)`；随即沿用现有 seen 切片（`:168-172`）+ `collapseChapters`（`:173`）。

> 注：`FetchNovelsOptions.maxLatestWindow` 默认 50（`fetcher.ts:16`），`config` 默认改 200 后由 `source.tsx:41,59` 透传；调用方若省略 `options` 则用 50。

### 3.2 merge O(n²)（锚点 `merge.ts:138-152` / `:83-98` / `:10-12`）

**方案**：

1. **去两处 `findIndex`**：在 `toNodes`（`:24-41`）构建节点时同步建 `const nodeByVariant = new Map<NovelChapterVariant, number>()`，`nodeByVariant.set(v, nodes.length)`（variant 引用在 `nodes` 中唯一，故平铺 Map 即可，无需文档提议的嵌套 `Map<sourceIndex, Map<...>>`——等价且更简单）。
   - spine 循环（`:138-144`）：`const nodeIdx = nodeByVariant.get(v)!` 替换 `nodes.findIndex(...)`。
   - extras 循环（`:149-162`）：同法替换。
   - 复杂度由 O(n²) → O(n)。
2. **空 `textKey` 跳过大桶**：`textMap` 构建循环（`:83-88`）加 `if (!n.textKey) continue`——纯「第N章」式标题归一化后 `textKey` 为 `''`（`textKeyOf` 见 `:10-12`），否则会聚成单一巨型桶，桶内两两 `union`（`:89-98`）退化为 O(k²)。编号章已按 `numKey` 在 `:70-78` 合并，跳过其 text-bridging 不影响正确性。
3. `textMap` 桶内两两 `union`（`:89-98`）保留：跳过空桶后各桶为「同归一化文本」的小集合（如多个「夜奔」），k 有界，不再退化。

### 3.3 并发收口（锚点 `app/refresh.ts:89`）

**现状**：`novels/fetcher.ts` 已在 S3 接入 `mapLimit`（`:43` 书级 `MAX_BOOK_CONCURRENCY=4`、`:93` 源级 `MAX_MIRROR_CONCURRENCY=3`）；`concurrency.ts` 的 `mapLimit(items, mapper, limit)` 已就绪。残余只在 `app/refresh.ts:89` `await Promise.all(stale.map((s) => refreshOne(s)))`。

**方案**：`app/refresh.ts` 引入 `mapLimit`，`:89` 改为 `await mapLimit(stale, refreshOne, MAX_REFRESH_CONCURRENCY)`，新增常量 `const MAX_REFRESH_CONCURRENCY = 4`。失败快速语义与现有 `Promise.all` 一致（不引入 `allSettled`，属范围外）。

### 3.4 SafeLine PoW 主线程 + 超时（锚点 `safeline.ts:182,191-209` / `weather/api.ts:100,136`）

**现状**：`solveSafelinePow(prefix, leadingZeroBits)`（`:191`）同步循环最多 `MAX_POW_ITERATIONS=1_000_000`（`:182`），每轮 `sha1Hex(prefix+suffix)`（`:38`，每次新建 `msg: number[]` + `new Uint32Array(80)` + hex 字符串）。难度 20 bit ≈ 1M 次 SHA-1 同步跑 → 主线程冻结。超限返回 `'0'`（`:208`）→ `weather/api.ts:100-109` 仍发一次注定失败的重试。

**方案**：

1. **异步 + 主线程让出（复用 S4 `runtime.setTimeout`）**：签名改为
   ```ts
   export type PowYield = () => Promise<void>
   export async function solveSafelinePow(
     prefix: string,
     leadingZeroBits: number,
     options: { yieldEvery?: number; maxIterations?: number; yield?: PowYield } = {},
   ): Promise<string>
   ```

   - 默认 `yield = () => Promise.resolve()`（测试用，不真正让出）；每 `yieldEvery`（默认 4096）次迭代 `await yield()`。
   - 调用点 `weather/api.ts:100`：`const suffix = await solveSafelinePow(challenge.prefix, challenge.leadingZeroBits, { yield: () => new Promise<void>((res) => runtime.setTimeout(res, 0)) })`。借 S4 `runtime.setTimeout` 在浏览器事件循环上让出，主线程不冻结。
2. **难度护栏**：`if (leadingZeroBits > MAX_DIFFICULTY_BITS) throw new Error(...)`（新增 `const MAX_DIFFICULTY_BITS = 20`，对应「20 bit ≈ 1M」警戒线）。超过直接放弃，不发重试。
3. **超限改抛错**：超过 `maxIterations`（默认 `MAX_POW_ITERATIONS=1_000_000`）改 `throw`（原返回 `'0'`），走 `weather/api.ts:136` `Promise.allSettled` 的 open-meteo 降级分支，不再发注定失败的重试。
4. **热循环优化（降常数）**：
   - 抽出核心 `sha1Digest(msg: Uint8Array): Uint8Array`（20 字节），`sha1Hex(input)` 改为 `bytesToHex(sha1Digest(asciiBytes(input)))`——保留同步导出与既有 FIPS 测试向量不变。
   - `sha1Digest` 复用模块级 `const W = new Uint32Array(80)`（单线程、solve 内 `await` 让出仍安全）。
   - 热循环用 `hasLeadingZeroBitsBytes(digest: Uint8Array, bits)` 直接查摘要前导零位，**不再构造 hex 字符串**；`hasLeadingZeroBits(hex, bits)`（测试用）重写为基于 `hasLeadingZeroBitsBytes(hexToBytes(hex), bits)`，避免重复实现。
   - `prefix` 字节预计算一次，每轮仅追加 `cnt.toString(16)` 的字节后做 `sha1Digest`（小缓冲分配）。

**测试影响**：`test/prism/weather/safeline.test.ts` 的 `solveSafelinePow` 用例改为 `await`；新增：(a) 高难度护栏 `leadingZeroBits: 25` → `rejects`；(b) `maxIterations: 1` 小值 + 难解前缀 → `rejects`（确定性验证「超限抛错」）；(c) 低难度异步仍返回合法后缀且 `hasLeadingZeroBits` 成立。

## 4. 分步实施

1. **`merge.ts`**：增 `windowLatestChapters`（§3.1.4）；`MergeOptions` 增 `maxWindow`（默认 `Infinity`）；`toNodes` 建 `nodeByVariant`（§3.2.1）；`textMap` 循环 `if (!n.textKey) continue`（§3.2.2）；spine/extras 两处 `findIndex` → `nodeByVariant.get`（§3.2.1）；`mergeSourceChapters` 在 seen 切片前调用 `windowLatestChapters`。
2. **`fetcher.ts`**：`fetchOneBook` `_options`→`options`，取 `maxLatestWindow`（§3.1.1）；`fetchSourceChapters` 增 `maxLatestWindow` 形参 + 尾页收集器与收口（§3.1.2）；`:168` 透传 `{ maxWindow: maxLatestWindow }`（§3.1.3）。
3. **`config/defaults.ts`**：`:38` `Infinity` → `200`（§3.1.4，决策点见 §3.1）。
4. **`app/refresh.ts`**：引入 `mapLimit`，`:89` 改 `mapLimit(stale, refreshOne, MAX_REFRESH_CONCURRENCY)`（§3.3）。
5. **`safeline.ts`**：抽出 `sha1Digest`/`asciiBytes`/`hexToBytes`/`hasLeadingZeroBitsBytes`，重写 `sha1Hex`/`hasLeadingZeroBits` 复用（§3.4.4）；`solveSafelinePow` 改异步 + `yield` + 难度护栏 + 超限抛错（§3.4.1/2/3）。
6. **`weather/api.ts`**：`:100` `await solveSafelinePow(...)` 并传 `runtime` 让出（§3.4.1）。
7. **测试**：`merge.test.ts` 增 `windowLatestChapters` 用例 + 大输入合并正确性用例（验证 O(n) 不退化）；`safeline.test.ts` `solve` 用例改 `await` + 护栏/超限抛错用例（§3.4 测试影响）。
8. **`bun run check`**：typecheck + lint + format + test + build 全绿。

## 5. 验证

- `bun run check`（项目统一门禁）。
- `merge.test.ts`：
  - `windowLatestChapters`：有限窗口截断且保留已读边界章；`Infinity` 原样；
  - 大输入（如 2 源 × 800 章 = 1600 节点）合并结果正确（验证 findIndex→Map 等价、空 textKey 跳桶正确）。
- `safeline.test.ts`：既有 sha1/零位/解析用例不变；`solve` 异步用例仍满足 `hasLeadingZeroBits`；难度护栏与超限抛错（`maxIterations:1`）确定性通过。
- `app/refresh.ts` 行为不变（仅并发有界）；`novels/fetcher.test.ts`、`multi-source-real.test.ts` 仍绿（窗口默认 50/200 不破坏既有断言——若既有用例依赖全量章节，需确认其 `options.maxLatestWindow` 设为 `Infinity`）。

## 6. 决策记录 / 开放问题

- **§3.1.4 `defaults.ts` 默认 = `200`（已拍板）**：按文档建议改有限默认 200；未配置用户得到 200 章窗口上限。
- **§3.4 难度护栏 = `20` bit（已拍板）**：`MAX_DIFFICULTY_BITS = 20`（对应「20 bit ≈ 1M」警戒）。>20 bit 视为服务端误配，直接放弃走降级。
- **§3.4 是否引入 `crypto.subtle.digest` 长线方案**：S5 不做（属 §3.4「长期」），沿用同步 SHA-1 + 异步让出。
- **§3.3 `mapLimit` 是否改 `allSettled`**：S5 不改（保持与现有 `Promise.all` 相同的 fail-fast 语义）。
- 计划已审批并实施。

## 7. 完成状态（2026-09-03）

S5 已完整落地，非「只设计」。实际改动与计划偏差在此登记：

### 7.1 已落地

- **§3.1 `maxLatestWindow` 接线**：`fetchOneBook` 形参 `_options`→`options`；`fetchSourceChapters` 增 `maxLatestWindow` 形参；尾页循环改为「首页够则不翻页（`homeFillsWindow`）+ 收集器 + 达窗口即 break + 单次 `reverse().flat()`」（消除逐页 `...reverse()` 全量复制）；`mergeSourceChapters` 透传 `{ maxWindow }`。
- **§3.1.4 `windowLatestChapters`**：新增纯函数，限长并保留已读边界章。`mergeSourceChapters` 改为**先 `collapseChapters` 再限窗口**（限长在后，避免截断最旧边界章——这修正了初版「先限窗口」导致 `multi-source-real` 测试 `n:1` 丢失的问题）。
- **§3.2 merge O(n²)**：`toNodes`/merge 内建 `nodeByVariant: Map<variant, nodeIdx>` 替换两处 `nodes.findIndex`（O(n²)→O(n)）；`textMap` 建桶时 `if (!n.textKey) continue` 跳过纯「第N章」空桶，消除 O(k²)。
- **§3.3 并发收口**：`app/refresh.ts:89` 裸 `Promise.all` → `mapLimit(stale, refreshOne, MAX_REFRESH_CONCURRENCY=4)`（`novels/fetcher.ts` 已在 S3 接入，本次仅补此残余点）。
- **§3.4 SafeLine PoW**：抽 `sha1Digest(bytes)`/`asciiBytes`/`bytesToHex`/`hasLeadingZeroBitsBytes`，`sha1Hex` 复用核心；热循环免 hex 字符串、复用 `W = new Uint32Array(80)`；`solveSafelinePow` 改异步（`yieldEvery=4096`），借 S4 `runtime.setTimeout` 主线程让出；难度护栏 `MAX_DIFFICULTY_BITS=20` 超则 `throw`；超限改 `throw`（走 `weather/api.ts:136` `Promise.allSettled` 降级，不再发注定失败重试）。`weather/api.ts:100` `await` + 传 `runtime` 让出；`scripts/fetchers/verify-cma.ts` 同步调用改 `await`。

### 7.2 文档/测试

- `config/defaults.ts:38` `maxLatestWindow: Infinity` → `200`（已拍板）。
- `merge.test.ts`：新增 `windowLatestChapters` 5 用例 + 大输入合并正确性用例（2 源 × 500 章，验证 findIndex→Map 等价）；`maxWindow` 选项用例。
- `safeline.test.ts`：`solve` 用例改 `await` + 难度护栏/超限抛错用例 + `hasLeadingZeroBitsBytes` 用例。
- 注：`mergeSourceChapters` 限长**先于 collapse** 因初版「先限后 collapse」会截断最旧章、破坏 `multi-source-real` 的 `n:1` 断言；改为「先 collapse 后限长」既保留最旧边界又满足 `章节窗口` 语义。

### 7.3 验证

- `bun run check` 全绿：lint ✓ / typecheck ✓ / format ✓ / test **1446 pass / 0 fail**（较 S4 的 1435 +11 项 S5 新测试）/ build ✓（prism hash `0a568a92`）。
- 改动未提交（按 AGENTS.md 不自动 commit）。待用户 `commit` 推进 S6。
