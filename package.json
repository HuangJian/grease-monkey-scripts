{
  "name": "grease-monkey-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run scripts/test-silent.ts",
    "test:verbose": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun run scripts/build-userscript.ts",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "check": "bun run typecheck && bun run lint && bun run format && bun run test && bun run build"
  },
  "dependencies": {
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.2",
    "@testing-library/preact": "^3.2.4",
    "@types/bun": "^1.3.14",
    "@types/jsdom": "^28.0.3",
    "jsdom": "^29.1.1",
    "lightningcss": "^1.32.0",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0",
    "typescript": "^6.0.3"
  }
}
