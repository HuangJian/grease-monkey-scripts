{
  "name": "grease-monkey-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun run scripts/build-userscript.ts",
    "check": "bun run typecheck && bun test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/jsdom": "^21.1.7",
    "jsdom": "^24.1.3",
    "typescript": "^5.8.3"
  }
}
