# S14 — §4.2 收尾：xit 模块裸时钟收敛（C 类 9 处）

> 设计先行：本文件待 review，实施前不改代码。S13 已提交 `58e70f5`（A+B 类 8 处 + 确定性单测），工作树干净。

## 0. 范围与前提

S13.plan.md §1.2 C 类列出 xit 模块 9 处裸时钟，因"逐层加参数 + 动 152 条既有用例"的顾虑拆到本阶段。

**核心设计修正（相对 S13.plan.md §3 S13.3 的悲观判断）**：这 9 处不需要重写 152 条用例。采用与 S13 B 类一致的**默认参数 seam**——每个读钟函数加 `now: Date = new Date()`（或其调用方已有的 `now?`）参数，默认回落到真实时钟。这样：

- **既有 152 条 xit 用例零改动**：它们不传 `now`，走默认真实时钟，行为完全不变。
- **宿主边界收敛**：`runtime`-感知的入口（`component/body.tsx`、`source.tsx`）在调用时显式传 `new Date(runtime.now())`，裸时钟清零。
- **可测性**：新增少量确定性单测，用固定 `new Date(FIXED)` 断言输出，消除跨日/跨年 flaky（recurring-reset、due-date 最易在日期边界翻车）。
- **内部链一致性**：同一次查询/渲染内，`now` 沿调用链透传，避免一个调用里读两次墙钟导致微秒级不一致。

`xit` 函数大量返回/比较 `Date` 对象，故统一用 `Date` 类型透传（不是 `number`），边界处 `new Date(runtime.now())` 构造。这与 S13 B 类的 `new Date(now)`（now:number）同构，只是类型不同。

## 1. 现状盘点（9 处，file:line 精确，基于 `58e70f5`）

| #   | 位置                     | 函数                                                                    | 性质                                          |
| --- | ------------------------ | ----------------------------------------------------------------------- | --------------------------------------------- |
| 1   | `query/date-math.ts:5`   | `getTodayStart()` `const d = new Date()`                                | 真裸                                          |
| 2   | `query/date-math.ts:79`  | `parseDateValue()` `const year = new Date().getFullYear()`              | 真裸                                          |
| 3   | `query/match.ts:68`      | `matchDatePeriod()` `const today = new Date()`                          | 真裸                                          |
| 4   | `query/match.ts:89`      | `matchDatePeriod()` `const year = new Date().getFullYear()`             | 真裸                                          |
| 5   | `query/match.ts:128`     | `matchDatePeriod()` `const year = new Date().getFullYear()`             | 真裸                                          |
| 6   | `parser.ts:191`          | `parseDueDate()` everyday 分支 `const now = new Date()`                 | 真裸                                          |
| 7   | `render/due-date.ts:9`   | `getDueDateStatus()` `const today = new Date()`                         | 真裸                                          |
| 8   | `render/due-date.ts:34`  | `formatDueDateDisplay()` `const currentYear = new Date().getFullYear()` | 真裸                                          |
| 9   | `recurring-reset.ts:100` | `applyRecurringReset(text, lastReset, new Date())` 调用点               | 调用点传墙钟（被调函数已有 `now: Date` seam） |

**已有 seam、不动**：

- `parser.ts:175` `resolveWeekday(name, now?: Date)` —— `now ?? new Date()` 已注入。
- `recurring-reset.ts:44` `applyRecurringReset(..., now: Date)` —— 参数已存在，仅调用点（#9）传墙钟。
- `scoring-utils.ts:9` `now ?? new Date()` —— 在 xit 外，不在本阶段。

## 2. 调用图（决定 `now` 透传路径）

```
query 链（公共入口 filterItems）:
  filterItems(lines, ast)                  [match.ts:219]
    └ matchItem(item, ast)                [match.ts:167]
        ├ matchDate(item, op, value, off)        → resolveDateKeyword / parseDateValue / parseDueDate
        ├ matchDatePeriod(item, spec, off)      → resolveDateKeyword  + 3 处裸钟(#:3,4,5)
        └ matchDateKeyword(item, kw, off)        → resolveDateKeyword / parseDueDate
  resolveDateKeyword(kw, off)             [date-math.ts:25] → getTodayStart / resolveWeekday(已有now?)
  getTodayStart()                         [date-math.ts:4]  → 裸钟 #1
  parseDateValue(value)                   [date-math.ts:71] → 裸钟 #2
  parseDueDate(dateStr)                   [parser.ts:186]  → everyday 裸钟 #6

render 链（due-date 展示）:
  getDueDateStatus(dateStr)               [due-date.ts:3]  → 裸钟 #7 + parseDueDate
  formatDueDateDisplay(dateStr)           [due-date.ts:32] → 裸钟 #8
  调用方：render/list.tsx:70,71,156 与 component/body.tsx:19,22

recurring 链:
  resetRecurringTasks(runtime, text)      [recurring-reset.ts:98] → applyRecurringReset(..., new Date()) #9
```

**`runtime` 入口（已确认有 `runtime` 作用域）**：

- `component/body.tsx`：`XitBody` 收 `runtime` prop；`filterItems(lines, result.ast)` @:58、`getDueDateStatus` @:19,22、`computePinnedLines` 内部均在此文件且可拿到 `runtime`。
- `source.tsx`：`resetRecurringTasks(runtime, text)` @:51,59 已传 `runtime`，仅需把内部 `new Date()` 改为 `new Date(runtime.now())`。

## 3. 方案

### S14.1 纯函数加 `now: Date` 默认 seam（8 处真裸）

全部 `now: Date = new Date()`，避免类型在 Date/number 间反复转换；内部调用透传同一 `now`。

- **`query/date-math.ts`**
  - `getTodayStart(now: Date = new Date())` —— `:5` `new Date()` → `now`，其余 `d.getFullYear()` 等基于 `now`。
  - `resolveDateKeyword(kw, offset?, now: Date = new Date())` —— 透传 `now` 给 `getTodayStart(now)` 与 `resolveWeekday(name, now)`（已有 `now?`，直接传）。
  - `parseDateValue(value, now: Date = new Date())` —— `:79` `new Date().getFullYear()` → `now.getFullYear()`。
- **`query/match.ts`**（逐层透传）
  - `matchDate(item, op, value, offset?, now = new Date())` —— 透传 `now` 给 `resolveDateKeyword(value, offset, now)`、`parseDateValue(value, now)`、`parseDueDate(item.dueDate, now)`。
  - `matchDatePeriod(item, spec, offset?, now = new Date())` —— `:68` `new Date()` → `now`；`:89`/`:128` `new Date().getFullYear()` → `now.getFullYear()`；`resolveDateKeyword(spec, offset, now)` 透传；`parseDueDate(item.dueDate, now)` 透传。
  - `matchDateKeyword(item, kw, offset?, now = new Date())` —— `resolveDateKeyword(kw, offset, now)`、`parseDueDate(item.dueDate, now)` 透传。
  - `matchItem(item, ast, now = new Date())` —— 三个 `matchXxx` 均透传 `now`。
  - `filterItems(lines, ast, now = new Date())` —— 透传 `now` 给 `matchItem`。
- **`parser.ts`**
  - `parseDueDate(dateStr, now: Date = new Date())` —— `:191` everyday 分支 `new Date()` → `now`。
- **`render/due-date.ts`**
  - `getDueDateStatus(dateStr, now: Date = new Date())` —— `:9` `new Date()` → `now`（影响 `todayStart` 等构造）；`parseDueDate(dateStr, now)` 透传。
  - `formatDueDateDisplay(dateStr, now: Date = new Date())` —— `:34` `new Date().getFullYear()` → `now.getFullYear()`。

### S14.2 调用方注入 `runtime.now()`（runtime 入口）

- **`component/body.tsx`**
  - `:58` `filterItems(lines, result.ast)` → `filterItems(lines, result.ast, new Date(runtime.now()))`。
  - `computePinnedLines(displayLines)` 改签名 `computePinnedLines(displayLines, now: Date = new Date())`，`:19`/`:22` `getDueDateStatus(l.dueDate ?? '', now)`；调用点（`XitBody` 内，有 `runtime`）传 `new Date(runtime.now())`。
  - 直接调用 `getDueDateStatus` 处（若有）同样传 `now`。
- **`render/list.tsx`**：`XitList` 增加 `now?: Date` prop（默认 `new Date()`，保持既有渲染无感）；`parseDescriptionTokens(line, now)` 与 `XitItem` 的 `:156` `getDueDateStatus(line.dueDate, now)`、`getDueDateStatus`/`formatDueDateDisplay`（`:70,71`）透传 `now`。`body.tsx` 渲染 `<XitList ... now={new Date(runtime.now())} />`。
- **`source.tsx` / `recurring-reset.ts`**（#9）
  - `recurring-reset.ts:100` `applyRecurringReset(text, lastReset, new Date())` → `new Date(runtime.now())`（`resetRecurringTasks(runtime, text)` 作用域已有 `runtime`）。

### S14.3 补确定性时间单测（新增，不改动既有 152 例）

沿用 S13.3 模式（固定 `new Date(FIXED)`），覆盖 seam 生效：

- `test/prism/xit/query.test.ts`（若存在）或新建 `date-math`/`match` 测试：用固定 `now` 断言 `resolveDateKeyword('today')`、`parseDateValue('1231')`（取 now 年份）、`matchDatePeriod` 的 `thisweek`/季度/月份分支结果与固定 `now` 一致。
- `test/prism/xit/parser.test.ts`：断言 `parseDueDate('everyday', now)` 返回 `now` 的本地零点。
- `test/prism/xit/recurring-reset.test.ts`：已有测试用 `applyRecurringReset(text, lastReset, new Date(...))` 注入；补一例用 `runtime.setClock` 经 `resetRecurringTasks` 走完整链路（可选，验证边界收敛）。
- `test/prism/xit/pin.test.ts` / `due-date` 展示：用固定 `now` 断言 `getDueDateStatus`/`formatDueDateDisplay` 输出。

> 既有 152 例因默认 seam 行为不变，**不修改**；仅当某既有断言隐含"今天相对"且跨日 flaky 时才改为传固定 `now` 钉死（预计为 0，见到再说）。

## 4. 明确不动

- `parser.ts:175` `resolveWeekday(now?)`、`recurring-reset.ts:44` `applyRecurringReset(now)`、`scoring-utils.ts:9` —— 已有 seam。
- `new Date(ts)` 日期构造/解析（不读时钟）：`date-math.ts` 的 `addDays`/`addMonths`/`new Date(y,m,d)`、`parser.ts` 的 `YYYY-MM-DD` 等解析、`due-date.ts` 的 `todayStart` 构造（其输入是 `now`，已由 seam 控制）—— **不在本次范围**。
- 测试桩 `test/runtime.ts` 的 `setClock` 机制不动（S13 已就绪）。

## 5. 验收

1. `export PATH="/Users/hj/.bun/bin:$PATH" && bun run check`（typecheck + lint + format + tests + build）。
2. 定向（分批，规避 `bun test` 合并 SIGTERM(137)）：
   - `bun test test/prism/xit`
   - `bun test test/prism/sources`（如仍 SIGTERM，再拆子目录）
   - 相关渲染/组件：`bun test test/prism/xit/parser.test.ts test/prism/xit/recurring-reset.test.ts test/prism/xit/query.test.ts`
3. 回归核验：`src/prism/xit` 下裸 `new Date()`/`Date.now()` 应清零（仅剩默认参数 seam 的 `= new Date()` 回落值与注释）；`grep -rn "new Date()" src/prism/xit` 应只命中 `now: Date = new Date()` 形与 `new Date(runtime.now())` 边界注入。
4. 152 条既有 xit 用例应**全部保持通过**（默认 seam 保障），无回归。

## 6. 风险与备注

- **类型透传一致性**：`now` 统一用 `Date`；调用链中途切勿再 `new Date()`，否则回到裸钟。grep 验收即为此设防。
- **`list.tsx` 加 `now?` prop**：默认 `new Date()` 保证未传时行为不变；仅 `body.tsx` 入口传 `runtime.now()`。若评审认为 render 层无需确定性，可退化为只在 `body.tsx` 直接调用处传 `now`、`list.tsx` 保持真实时钟——但为统一收敛，本计划主张全链透传。
- **不自动 commit / push**：实施完报 message，等 `commit`；S14.plan.md 随阶段提交（对齐 S11/S12 先例）。

## 7. 落地记录（验收实际执行）

> 实施于 2026-09-09，遵循 `go` 批准。本会话由 S14 设计待审直接转入实施。

**代码改动（8 文件，纯函数 seam + 调用方注入）**
- `query/date-math.ts`：`getTodayStart`/`resolveDateKeyword`/`parseDateValue` 加 `now: Date = new Date()` 并全链透传。
- `parser.ts`：`parseDueDate(dateStr, now)`；everyday 分支与 `resolveWeekday(trimmed, now)` 透传。
- `query/match.ts`：`matchDate`/`matchDatePeriod`/`matchDateKeyword`/`matchItem`/`filterItems` 逐层加 `now` 透传（覆盖 #3/#4/#5 三处裸钟）。
- `render/due-date.ts`：`getDueDateStatus`/`formatDueDateDisplay` 加 `now`（#7/#8）。
- `recurring-reset.ts:100`：`new Date()` → `new Date(runtime.now())`（#9 边界注入）。
- `component/body.tsx`：`now = new Date(runtime.now())` 注入 `filterItems`/`computePinnedLines`/`ListContent`。
- `render/list.tsx`：`XitList` 加 `now?: Date`；透传 `XitItem`→`renderDescription`→`parseDescriptionTokens`→`getDueDateStatus`/`formatDueDateDisplay`。
- 新增 `test/prism/xit/deterministic-time.test.ts`（11 例，固定 `FIXED`/`NOW_LATER` 与 `setClock` 边界）。

**验收执行（沙箱内 `bun run check` 整体 SIGTERM(137)，改按组件分批）**
- `bun run typecheck` → ✅ 通过。
- scoped `oxlint` 7 src + 1 test → ✅ 0 warning / 0 error。
- scoped `oxfmt` 同 8 文件 → ✅ 无 diff（格式一致）。
- `bun run build` → ✅ prism 模块产出（hash 57ef948f）。
- `bun test test/prism/xit` → ✅ **163 pass / 0 fail**（152 既有 + 11 新增，零回归）。
- grep 回归：`src/prism/xit` 内 `new Date(` 仅剩 ① `= new Date()` 默认 seam、② `now ?? new Date()` 回落、③ 解析/算术构造 `new Date(y,m,d)`、④ `new Date(0)` epoch、⑤ `new Date(runtime.now())` 边界注入——**逻辑内无裸时钟读**。

**结论**：S14 的 9 处裸时钟（C 类）全部收敛，既有 152 例零改动且全绿，`runtime` 边界经 `setClock` 单测验证生效。未 commit / 未 push，等工作树待 `commit`。
