# S13 — §4.2 收尾：裸时钟收敛到 Runtime

> 设计先行：本文件待 review，实施前不改代码。S12 已提交 `8735bac`（§4.9 重试策略 + §4.2 残余定时器），工作树干净。

## 0. 前提勘误

`frontend.refactor.md:245` 原文：

> 28 个文件 / 60+ 处裸 `Date.now()`。

**实测已严重过时**（S1–S12 期间大量收敛已完成）：

- 裸 `Date.now()` 在 `src/prism` 下只剩 **5 文件 / 10 处**文本命中，其中 **3 处是注释文字**（`v2ex/parser.ts:52`、`v2ex/fetcher.ts:41`、`v2ex/fetcher.ts:98`），**5 处是"默认参数 = `Date.now()`"的注入 seam**，真正的裸调用只剩 **2 处**。
- 真正的大头换成了**无参 `new Date()`（取当前时间）**：**10 文件 / 18 处**。
- 与此同时 `runtime.now()` 已被 **23 个文件**采纳（约 36 处），模式成熟。

结论：§4.2 只剩"时钟"这半边没收，且规模远小于文档描述。

## 1. 现状盘点（已查证，file:line 精确）

### 1.1 裸 `Date.now()`（10 处命中 → 2 处需动）

| 位置                                                                  | 性质          | 处理   |
| --------------------------------------------------------------------- | ------------- | ------ |
| `item-state.ts:164` `expandedAt.set(id, Date.now())`                  | 真裸调用      | **动** |
| `item-state.ts:168` `expandedAt.set(id, Date.now())`                  | 真裸调用      | **动** |
| `item-state.ts:65` `markRead(id, ts = Date.now(), …)`                 | 默认参数 seam | 不动   |
| `item-state.ts:70` `markHidden(id, ts = Date.now())`                  | 默认参数 seam | 不动   |
| `shared/date-parse.ts:35` `currentYear(now = Date.now())`             | 默认参数 seam | 不动   |
| `shared/date-parse.ts:80` `parseChapterLabel(text, now = Date.now())` | 默认参数 seam | 不动   |
| `novels/adapters/sudugu.ts:61` `now: number = Date.now()`             | 默认参数 seam | 不动   |
| `v2ex/parser.ts:52`、`v2ex/fetcher.ts:41,98`                          | 注释文字      | 不动   |

> 默认参数 seam 与 `lock.ts` 既有的 `now?()` 注入同性质：调用方（含测试）可传值，宿主边界未被绕过，**不属于 §4.2 要解决的问题**。

### 1.2 无参 `new Date()`（18 处，按改造成本分三类）

**A 类 — `runtime` 已在作用域，直接替换（1 处）**

- `misc/openrouter/fetcher.ts:133` `fetchedAt: new Date().toISOString()`（在 `fetchOpenRouterModels(runtime, …)` 内）

**B 类 — 纯函数 / 无 runtime，需加 `now: number` 参数并改调用方（7 处）**

- `item-state.ts:164,168` —— `createExpandedState()` 无参；调用方 2 个：`xueqiu/state.ts:24`、`tnews/state.ts:25`（均在 runtime 作用域内）
- `shared/date-parse.ts:65` `const year = new Date().getFullYear()`
- `save-dialog.tsx:35` `formatSaveFilename()` 无参
- `export-import.ts:142` `formatExportFilename()` 无参
- `weather/cma/parse-now.ts:47` `new Date().toISOString()`（`time` 字段回落值）
- `novels/component.tsx:320` `formatPostedAt(ts)` 内取 `now`

**C 类 — xit 模块集中改造（9 处，建议拆 S14）**

- `xit/query/date-math.ts:5,79`
- `xit/query/match.ts:61,82,121`
- `xit/parser.ts:191`（注：`:175` 已有 `now ?? new Date()` seam，不动）
- `xit/recurring-reset.ts:100`
- `xit/render/due-date.ts:9,34`

**已有 seam、不动**：`scoring-utils.ts:9` `now ?? new Date()`、`xit/parser.ts:175` `now ?? new Date()`。

## 2. 为什么要做（判据）

- **宿主边界唯一**：`Runtime` 是 AGENTS.md 约定的唯一宿主边界，绕过它取时间等于把测试钉在真实时钟上。
- **可测性**：`test/runtime.ts` 已提供 `setClock(ms)`，收敛后时间相关用例可完全确定性重放，消除跨日/跨年的 flaky（例如 `xit` 的 due-date、recurring-reset 天然易在日期边界翻车）。
- **只动语义为"取当前时间"的调用**：`new Date(ts)` 这类**日期构造/解析**（`xit/query/date-math.ts` 大量、`shared/date-parse.ts` 主体）**不在本次范围** —— 它们不读时钟。

## 3. 方案

### S13.1 A 类（1 处）

`misc/openrouter/fetcher.ts:133` → `new Date(runtime.now()).toISOString()`。同步在 `test/prism/misc/openrouter.test.ts` 用 `runtime.setClock()` 断言 `fetchedAt` 固定值。

### S13.2 B 类（7 处，逐个加显式时间参数）

统一采用**显式 `now: number` 参数**（而非传整个 `runtime`），保持纯函数可测：

- `createExpandedState(now: () => number)` —— 与 `tryAcquireLock` 的 `now?()` 注入风格一致；`xueqiu/state.ts:24`、`tnews/state.ts:25` 传 `runtime.now`。
- `formatSaveFilename(now: number)`、`formatExportFilename(now: number)`、`formatPostedAt(ts, now)`、`parse-now` 与 `date-parse.ts:65` 的对应函数同法处理。

### S13.3 C 类（9 处）→ **建议拆到 S14**

xit 的 9 处分布在 `query/date-math`、`query/match`、`parser`、`recurring-reset`、`render/due-date` 五条链路上，且**该模块目前零 `now?: number` seam**（已确认 `src/prism/xit` 下无 `now?: number`），需要逐层加参数并同步改 `xit/component`、`xit/render` 的调用方与 xit 的 152 条既有用例。改动面与 S13.1/S13.2 不是一个量级，合并会显著放大单次 review 风险。

## 4. 明确不动

- 5 处"默认参数 = `Date.now()`"的注入 seam（§1.1 表）。
- `new Date(ts)` 等日期构造/解析调用（不读时钟）。
- 已有 `now ?? new Date()` seam 的 2 处。
- §4.12 遗留观察项（`novels/merge.ts` 的 `20/10` 展示折叠阈值）——文档定性为"留作后续观察"，非缺陷，不在本阶段。

## 5. 验收

1. `export PATH="/Users/hj/.bun/bin:$PATH" && bun run check`（typecheck + lint + format + tests + build）
2. 定向：`bun test test/prism/misc test/prism/item-state.test.ts test/prism/xueqiu test/prism/sources/tnews test/prism/export-import.test.ts`
3. 回归核验：`src/prism` 下裸 `Date.now()` 应只剩 5 处默认参数 seam + 3 处注释；A/B 类 8 处清零。
4. `bun test` 合并跑 `test/prism/sources` 仍会 SIGTERM(137)（沙箱资源限制，S11 已知非代码失败），改按目录分批跑。
