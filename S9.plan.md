# S9.plan.md — §4 架构收敛（依赖方向 + 模块级状态 + 接口完整性）

> 阶段：`S9` · 范围：`frontend.refactor.md` §4（P3 架构与可维护性）
> 角色：前端架构师 · 设计先行，本文档只给方案与改动点，落地等 `S9` 指令
> 前置：S1–S8 已提交（最新 `76fc22e`，working tree clean，领先 origin/main 14）
> 验证门槛：每子任务后跑 `bun run check`（lint 0 · typecheck 0 · format 0 · test 全绿），全程结束报告 Build hash

---

## 0. 已核实状态（纠正 review doc 误述）

本次动手前用 grep + 读源码复核了 §4 各项，发现 review doc 两处已不成立：

- **§4.12 `getTagCounts` 是死代码** — ❌ 误述。实际定义在 `xit/tag-counts.ts:3`，有 1 个调用方 `xit/component/header.tsx:5,41`，存活。
- **§4.12 `initialNewChapters` / `maxNewChaptersPerBook` 无效** — ❌ 误述。定义在 `types.ts:89-90`，被 `config/defaults.ts:36-37`、`config/validate.ts`、`novels/editor/*`、`novels/source.tsx`、`novels/fetcher.ts:12,35` 等多处活跃使用。`novels/merge.ts:4-5` 的 `20/10` 是**展示折叠**独立阈值（与编辑器"折叠阈值"是两回事），非 bug，留作后续观察，**不纳入 S9**。
- **§4.3 `types.ts` 非叶子** — review 称 `types.ts:35-39` 导入 5 个 feature 目录；实际 `types.ts:35-50` 从 **9 个** feature 目录导入（5 个 config 类型 + 9 个 `AnySource` data 类型），更严重。
- **§4.1 拆环 / §2.7 预览入口** — 已确认完成（`.oxlintrc.json:19` 的 `import/no-cycle:error` 门禁下 `bun run check` 全绿；`undefined as any` grep 零命中）。不在 S9 范围。

**真正剩余且纳入 S9 的子项**：§4.3、§4.4、§4.5、§4.6、§4.7、§4.9，以及 §4.12 中确为死代码的两处（`novels/state.ts` 的 `initialSeenKey`、`isNewChapter`）。

---

## 1. 子任务与改动点（按依赖顺序，最低风险先打底）

### S9.1 §4.12 死代码清理（最低风险）

- `src/prism/novels/state.ts`：删除 `initialSeenKey`（:4-9）与 `isNewChapter`（:11-21）。保留 `newChapters`（:23-28）、`newChapterCount`（:31-33）——有调用方。
- 在 `frontend.refactor.md` §4.12 处追加勘误注记（getTagCounts / 两 config 字段存活、merge 阈值独立）。
- 验证：`bun run check`（test 数量不变，无回归）。

### S9.2 §4.5 模块级可变状态

- **(a) `src/prism/shell/mount.tsx`**：删除 `const roots = new WeakMap<HTMLElement, ShadowRoot>()`（:29）、`getMountedRoot`（:31-33）、`roots.set(host, root)`（:56）。`OverlayHandle.root`（:20）已是挂载点访问入口；grep 确认 `getMountedRoot` **全仓仅定义、0 调用方**，删除零影响。
- **(b) `src/prism/xit/filters.ts`**：删除 `let nextId = 1`（:7）与 `bumpNextId`（:17-25）。新增纯函数 `highestFilterNum(store): number` 扫描 `/^f(\d+)$/` 取最大；`addFilter`（:64-65）改用 `` `f${highestFilterNum(store) + 1}` ``；`deduplicateIds`（:27-38）内部 `let max = highestFilterNum(store)` 后 `` `f${++max}` ``。保留 `/^f(\d+)$/` 假设（**不用 `crypto.randomUUID()`**，避免破坏原语义）；**无 API 变更**（调用方签名不变）。
- 验证：`bun run check` + xit filter 相关测试（如有）。

### S9.3 §4.6 xit 双份 header-state 初始化

- `src/prism/xit/component/header.tsx`：新增 `export function createXitHeaderState(): HeaderStateStore<XitHeaderState>` 封装 6 字段（`query/queryError/filterStore/showFilters/saveForm/editFilter`）。
- `src/prism/xit/source.tsx:18-25` 与 `src/prism/xit/render/index.tsx:13-20` 两处改为 `createXitHeaderState()`。**无新增依赖边**（两文件本就从 `header.tsx` 导入 `createHeaderState` / `XitHeaderState`）。
- 验证：`bun run check`。

### S9.4 §4.9 retry 下沉 `shared/request`

- `src/prism/shared/request.ts`：`RequestOptions`（:5-9）加 `retry?: { statuses: number[]; max: number; baseDelayMs?: number }`。`requestTextWithHeaders`（:24-51）命中 `statuses` 时：解析 `retry-after` 响应头（大小写不敏感），按 `BACKOFF_DELAYS_MS`（`types.ts:16`，当前**未使用**→本次接上）或 `baseDelayMs` 退避，最多 `max` 次递归重试；超次则 reject。
- `src/prism/xueqiu/fetcher.ts`：当前 **0 重试** → 对主抓取接入 `retry: { statuses: [429], max: 2 }`（真实补强）；保留 `waitJitter`（:64-71）礼貌延迟。
- `src/prism/reddit/fetcher.ts`：`fetchOneSub`（:18-105）的 429 重试（:54-58）改走 `shared/request` 的 retry；**host-rotation**（`advanceHost`，:60-63 / :94-102，处理 403/网络/超时）保留为 reddit 特有逻辑（不通用，留本地）。删本地 `parseRetryAfter`（:6-12），`MAX_RETRIES_ON_429`（`reddit/constants.ts:9`）作为 `retry.max` 传入。
- 验证：`bun run check` + 现有 reddit/xueqiu 测试；新增 `test/prism/shared/request-retry.test.ts`（fake Runtime 注入 429 后 200，断言退避+成功；429 超次断言 reject）。

### S9.5 §4.4 feature / shell 反向依赖剥离（中等风险，最高价值）

- **(a) `src/prism/types.ts`**：新增结构类型 `ShowEditorDialog`（签名对齐 `shell/editor.tsx:86-94`：`(root: ShadowRoot, title: string | VNode, runtime: Runtime, renderEditor: (container, close) => SourceEditorResult | Promise<SourceEditorResult>) => () => void`——仅依赖 `preact`/`runtime`，**无 shell 导入**）。`SourceComponentProps<T>`（:143-152）加 `showEditorDialog?: ShowEditorDialog`。
- **(b) 新文件 `src/prism/card/edit-handler.ts`**：从 `shell/editor.tsx:114-149` 迁入 `createEditHandler` + `EditHandlerArgs`，**不 import shell**——`showEditorDialog` 作为 `EditHandlerArgs` 新增字段传入；从 `../types` 导入 `Source/SourceSettings/CONFIG_KEY/getSourceSettings`。
- **(c) `src/prism/shell/editor.tsx`**：删 `createEditHandler`/`EditHandlerArgs`（保留 `showEditorDialog`/`EditorDialog`），`showEditorDialog` 标注 `: ShowEditorDialog`。
- **(d) `src/prism/card/card.tsx`**：删 `import { createEditHandler } from '../shell/editor'`（:6）→ `import { createEditHandler } from './edit-handler'`；`RenderCard`（:35-100）接收 `showEditorDialog` prop（由 CardGroup 透传），`onEdit = createEditHandler({ ..., showEditorDialog })`（:49-55），`<Comp>` 透传 `showEditorDialog={showEditorDialog}`（:98）。→ **card→shell 边消除** ✅
- **(e) `src/prism/card/card-group.ts` + `tabs-card.tsx`**：加 `showEditorDialog` prop 并透传至 `RenderCard`。
- **(f) `src/prism/app/index.tsx`**（组合根，可 import shell）：`import { showEditorDialog } from '../shell/editor'`，构建 `CardGroup` 时传入 `showEditorDialog`。
- **(g) `src/prism/xit/component/body.tsx`**：删 `import { showEditorDialog } from '../../shell/editor'`（:4）；`openEditor`（:81-99）改 `props.showEditorDialog?.(root, title, runtime, async (b, c) => createEditor(lineIndex ?? null)(b, { runtime, onRevert: () => {}, close: c }))`，`showEditorDialog` 经 `SourceComponentProps` 注入。→ **feature→shell 边消除** ✅
- **(h) `src/prism/card/icons.tsx` → `src/prism/shared/icons.tsx`**：整体移动 7 个图标（`RefreshIcon/SaveIcon/ConfigIcon/EditIcon/DeleteIcon/ListIcon/SparklesIcon`）；更新 2 个导入方 `xueqiu/source.tsx:14`、`xit/component/header.tsx:17`（路径改为 `../shared/icons` / `../../shared/icons`）。
- **已知残留（本次不强制）**：`v2ex/component.tsx:5` 与 `reddit/component.tsx:5` 的 `import { ItemActions } from '../card/primitives'`（feature→card）属同类反向依赖，但 `ItemActions` 是通用 UI 原语；本次留作已知残留（后续把 `primitives` 上移 `shared` 统一处理），plan 标注即可。
- 验证：`bun run check` + 现有 render 测试（确认 xit 双击编辑、header 编辑按钮仍工作）；`app/index.tsx` 注入 `showEditorDialog` 后无 `undefined` 调用。

### S9.6 §4.3 `types.ts` 叶子化（最高 churn，最后做）

- **新建 `src/prism/config/types.ts`**：移入 `Config`（:72-106）、`RedditConfig`（:52-59）、`HupuConfig`（:61-70）+ re-export 5 个 feature config 类型（`WeatherCity`/`NovelBookConfig`/`TnewsConfig`/`MiscOptions`/`XueqiuSourceOptions`，从各自 feature dir 导入——config 是组合根，允许导入 feature config）。
- **新建 `src/prism/source-types.ts`**（prism 根，跨 feature 聚合的**唯一 sanctioned 例外**模块）：移入 `AnySource`（:196-206）+ `readSourceData`（:230-235），导入 `./types`（`Source`/`CachedSource`）+ 9 个 feature data 类型。
- **`src/prism/types.ts` 收敛为叶子**：仅保留 `Source`/`SourceComponentProps`/`SourceHeaderProps`/`CachedSource`/`Lock`/常量/`TabLabel`/`SourceSettings`/`BadgeType`/`SourceEditor*`/`ShowEditorDialog`；导入仅 `preact` / `../runtime` / `./keys`（re-export，合法）/ `./config/types`（`Config`）/**不 re-export `AnySource`**（避免重新引入 feature 导入）。
- **机械更新导入方**：
  - `Config` 8 处 → `../config/types`（`export-import.ts:2`、`app/source-registry.ts:11`、`app/index.ts:2`、`app/host-allowlist.ts:1`、`app/shortcut-bootstrap.ts:4`、`config/validate.ts:3`、`config/defaults.ts:1`、`config/load.ts:2`）。其中 `config/validate.ts` 还需从 `../types` 取 `SourceSettings`。
  - `AnySource` 3 处 → `../source-types`（`card-group.ts`、`app/source-registry.ts:11`、`app/sync-bootstrap.ts`）；`readSourceData` 1 处 → `../source-types`（`card/card.tsx:4`）。测试文件中的导入一并改。
- 验证：`bun run check`（**typecheck 0 为硬门槛**）；若 `exactOptionalPropertyTypes` 在边界报 TS2345/TS2375，按 S8 既定策略在单点用 `as unknown as` 收敛并记录（不扩散）。

### S9.7 §4.7 `Source` 接口补全（文档化，无功能变更）

- `Source<T,Id>` 已含可选 `loadState`/`RenderHeader`/`getTabLabel`；调用方 `s.loadState?.(runtime)`（`app/index.ts:129`、`sync-bootstrap.ts:19`）静默跳过——缺失 = 不预载，非 bug。
- 动作：`weather/source.tsx`、`misc/source.tsx` 显式写 `loadState: undefined, // 无预载状态（AGENTS.md 可选字段省略约定）`；`novels/source.tsx` 注释说明状态在 `fetch` 内派生故省略。
- 新增编译期 `SourceShape` 清单（type-level，断言 10 个成员均满足 `Source` 接口）于 `source-registry.ts` 或 `types.ts`，防未来回归。
- 验证：`bun run check`。

---

## 2. 提交与回调策略

- S9 一次性 commit（与 S7/S8 同节奏），message 列明覆盖的 § 编号。
- 若 S9.6（§4.3）导入面过大导致 `typecheck` 不稳定，实施时会先单独验证 S9.6 再并入，present 时标注；不自动拆分 commit，除非用户要求。
- 不自动 `git push`；本地领先 `origin/main` 14 → 15 commits，等用户 `push`。

---

## 3. 风险汇总

| 子项            | 风险  | 关键风险点                                                           |
| --------------- | ----- | -------------------------------------------------------------------- |
| S9.1 死代码     | 低    | 删前 grep 二次确认 0 调用方                                          |
| S9.2 模块状态   | 低-中 | xit/filters 改为每次重算 max，需保持 `f(\d+)` 假设                   |
| S9.3 xit header | 低    | 仅提取工厂，行为不变                                                 |
| S9.9 retry      | 中    | reddit host-rotation 与 shared retry 交界处语义；xueqiu 首次接入重试 |
| S9.5 反向依赖   | 中-高 | xit 双击编辑改经 `props.showEditorDialog` 注入；app 注入点需 trace   |
| S9.6 types 叶子 | 高    | 9+8 处导入机械改，typecheck 边界 `as`                                |
| S9.7 接口文档   | 低    | 仅注释 + 编译期断言                                                  |

优先级最高、收益最系统的是 **S9.5 + S9.6**（恢复依赖方向、types.ts 真叶子）；最低风险可立即打底的是 **S9.1 + S9.2 + S9.3**。

---

## 4. 不在 S9 范围（后续阶段）

- §4.12 `novels/merge.ts` 的 20/10 展示折叠阈值是否改接 `maxNewChaptersPerBook`（独立决策，需产品确认）。
- §4.4 残留 `v2ex/reddit` 的 `ItemActions` from `card/primitives`（feature→card），待 `primitives` 上移 `shared`。
- §3.5 / §4.10 / §4.13 / §5.3 / §2.4（用户此前在 AskUserQuestion 中未选中的其他线）。
