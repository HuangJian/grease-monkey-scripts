# S12 — §4.9 重试策略收尾

> 设计先行：本文件待 review，实施前不改代码。S11 已提交 `aa8aa6c`，`S5.plan.md` 文档修正已提交 `7c0f722`，工作树干净。

## 0. 前提勘误（必须先纠偏）

`frontend.refactor.md:290` 原文：

> 把 429 退避下沉到 4.8 的统一 `requestText`，用 `opts.retry?: {statuses, max}` 声明式开启。

**实测：下沉已完成，本阶段不是"下沉"。**

- 下沉层已存在：`src/prism/shared/request.ts:6-19`（`RetryOptions`）、`:44-54`（`computeRetryDelayMs`）、`:68-104`（`requestTextWithHeaders` 内重试循环）。`b0d0cba` 引入，S9 (`d2e0f22`) 定稿。
- 唯一声明式消费者：`xueqiu/fetcher.ts:77-80` `retry: { statuses: [429], max: 2 }`。

因此 §4.9 真正剩余的是两件事：

1. **下沉层自身的两个缺陷**（默认退避量级错误 / 无总预算）——不修就开启 = 把缺陷扩散到更多调用点。
2. **哪些调用点该开启**——目前 12 个 prism 请求点里只有 1 个开了。

## 1. 现状盘点（已查证，file:line 精确）

| #   | 调用点                          | 形态                                                   | 当前 retry                          |
| --- | ------------------------------- | ------------------------------------------------------ | ----------------------------------- |
| 1   | `xueqiu/fetcher.ts:77`          | `requestTextWithHeaders`                               | `{statuses:[429],max:2}` ✅         |
| 2   | `reddit/fetcher.ts:40`          | 裸 `runtime.request`                                   | 自有 429 重试 + 403→`advanceHost()` |
| 3   | `v2ex/fetcher.ts:79`            | `requestText`                                          | 无                                  |
| 4   | `hupu/fetcher.ts:20`            | `requestText`                                          | 无                                  |
| 5   | `tnews/fetcher.ts:12`           | `requestText`                                          | 无                                  |
| 6   | `novels/fetcher.ts:338`         | `requestText`（在 `fetchWithFallback` 换 host 循环内） | 无（但有跨 host fallback）          |
| 7   | `misc/openrouter/fetcher.ts:30` | `requestJson`                                          | 无                                  |
| 8   | `weather/api.ts:58`             | `requestJson`（open-meteo forecast）                   | 无                                  |
| 9   | `weather/api.ts:83`             | `requestTextWithHeaders`（cma）                        | 无                                  |
| 10  | `weather/api.ts:107`            | `requestTextWithHeaders`（cma safeline 二次请求）      | 无                                  |
| 11  | `weather/air-quality.ts:34`     | `requestJson`（open-meteo air-quality）                | 无                                  |
| 12  | `xueqiu/ai/summarize.ts:101`    | 裸 `runtime.request`（POST）                           | 无（S11 已判定排除）                |

测试现状：`test/prism/shared/request-retry.test.ts` 6 个用例（3 个 `computeRetryDelayMs` + 3 个端到端重试）。

## 2. 下沉层的两个缺陷

### 缺陷 A — 默认退避复用了 source 级 backoff，量级差 ~60 倍（必修）

`computeRetryDelayMs` 的回落链是 `Retry-After` → `baseDelayMs` → `BACKOFF_DELAYS_MS`（`request.ts:44-54`）。
而 `BACKOFF_DELAYS_MS = [60_000, 120_000, 300_000, 600_000]`（`types.ts:16`）是**整源刷新调度**用的（`app/refresh.ts:52` `nextRetryAt = now + computeBackoffMs(failureCount)`）。

后果：任一调用点收到**不带 `Retry-After` 的 429**，单次请求内部就要睡 60s；`max:2` 下累计 **180s**。期间一直占用 §3.3 `mapLimit` 的并发槽，且延长整个刷新周期。

`test/prism/shared/request-retry.test.ts:45-50` 目前**正在断言**该行为（`toBe(60_000)` / `120_000` / `600_000`），因此修改是 deliberate 的、测试可见的变更，需你确认。

### 缺陷 B — 无总时长预算

每次 `attempt(n)` 都重新带 `options.timeout` 发起，最坏耗时 = `(1+max) × timeout + Σbackoff`。默认 15s / openrouter 20s，`max:2` → 45s~60s + 退避。

## 3. 方案

### S12.1 退避阶梯与 source 级脱钩

`shared/request.ts` 新增 HTTP 专用阶梯，替换 `computeRetryDelayMs` 的回落终点：

```ts
/** HTTP-level retry backoff (seconds), distinct from source-level BACKOFF_DELAYS_MS (minutes). */
export const HTTP_RETRY_DELAYS_MS = [1_000, 4_000, 10_000] as const
```

优先级不变：`Retry-After` → `baseDelayMs` → 新阶梯。服务端给的 `Retry-After` 仍被完整尊重（这是正确行为，即使很长）。

同时更新 `request-retry.test.ts:45-50` 的断言到新阶梯（含 `attempt` 越界 clamp）。

### S12.2 分档开启（核心决策点，需你拍板）

判据：① 幂等 GET；② 429 现实存在（API 有限流）；③ 无自有重试/fallback 协议。

| 调用点                          | 结论           | 理由                                                                  |
| ------------------------------- | -------------- | --------------------------------------------------------------------- |
| `misc/openrouter/fetcher.ts:30` | **开 `max:1`** | LLM API 明确限流；但 timeout=20s 且属交互路径，故压到 1（呼应缺陷 B） |
| `weather/api.ts:58`             | **开 `max:1`** | open-meteo 免费额度限流                                               |
| `weather/air-quality.ts:34`     | **开 `max:1`** | 同上                                                                  |
| `v2ex/fetcher.ts:79`            | **开 `max:1`** | v2ex API 有限流                                                       |
| `xueqiu/fetcher.ts:77`          | 保持 `max:2`   | 已是声明式；改阶梯后最坏耗时自然收敛                                  |
| `tnews/fetcher.ts:12`           | **不开**       | 静态 RSS，429 概率低；失败由 source 级 `nextRetryAt` 兜底             |
| `hupu/fetcher.ts:20`            | **不开**       | 429 语义是"对方要求退避"，立即重试是错误礼仪；交给 `nextRetryAt`      |
| `novels/fetcher.ts:338`         | **不开**       | 已有跨 host `fetchWithFallback`，重试语义被 fallback 覆盖             |
| `weather/api.ts:83,107`         | **不开**       | cma/safeline 已有自有 challenge→retry 协议，叠加会混淆两条重试链      |
| `reddit/fetcher.ts:40`          | **保持自有**   | 403→`advanceHost()` 需换 URL；统一层只重试同一 URL，无法表达          |
| `xueqiu/ai/summarize.ts:101`    | **不开**       | POST 非幂等，S11 已排除                                               |

风险说明：若目标站点从不返回 429，本项改动是**纯 no-op**；唯一代价是 429 真发生时的最坏耗时，且已由 S12.1 从分钟级压到秒级。

### S12.3 总预算（倾向不做，待你定）

- **倾向：不引入 `retry.totalBudgetMs`。** 理由：`max ≤ 2` 且阶梯已收敛到秒级后，最坏 ≈ `3×timeout + 5s`，属可接受量级；加预算字段要引入 `runtime.now()` 时间源与新的失败分支，收益不抵复杂度（YAGNI）。
- 若你认为 60s 仍不可接受（尤其是 openrouter），替代方案是把 openrouter 的 `max` 降到 1（已含在上表）或引入预算字段。

### S12.4 测试

1. 更新 `request-retry.test.ts:45-50`：断言新阶梯 `1_000 / 4_000 / 10_000` 与越界 clamp。
2. 新增：`requestText`（非仅 `WithHeaders`）与 `requestJson` 透传 `retry` —— 覆盖 429→200 与 429 耗尽后 reject 两条路径（`requestJson` 走 `requestText` 委托，一条即可覆盖）。
3. 可选：`misc/openrouter` 目前**无测试文件**，可补一个 429→retry→200 的用例验证接线；不做也行，S12.2 的改动本身只是加一个 options 字面量。

## 4. §4.2 残余裸定时器（原建议 S13，用户「全部处理掉」后并入本阶段同步实施）

§4.2「Runtime 收敛定时器与时钟」**并未收干净**。文档列了 8 处裸定时器，现仍剩 5 处绕过 `runtime.setTimeout/setInterval`：

- `lock.ts:52`（`tryAcquireLock` 的 50ms 校验延时，`runtime` 已在作用域内，可直接换）
- `xueqiu/fetcher.ts:71`（`waitJitter`）、`:95`（`pageFetchJson` 超时守卫）
- `card/primitives.tsx:43-44`（`setInterval`/`clearInterval`）
- `xueqiu/component.tsx:121,183-184,191`（`setInterval`/`clearInterval`）

已确认**已完成**的部分：`test/prism/scheduling.test.ts` 的 `globalThis.setTimeout` 全局 patch 已删除（文档 `:247` 要求的清理已做）。

实施代价（已确认可控）：`lock.ts:52` 改用 `runtime.setTimeout` 后，`test/prism/lock.test.ts` 必须用假时钟驱动（`createRuntime()` 的 `setTimeout` 是 fake clock）——原 8 处调用统一加 `verifyDelayMs: 0`（它们断言的是 owner/TTL，与延时无关），另补 1 条用例专门验证延时确实注册在 Runtime 上；组件内 `setInterval` 只需把 `timerRef` 类型从 `ReturnType<typeof setInterval>` 收紧为 `number`。

**实测暴露的连锁问题（关键）**：`app/refresh.ts:18` 以默认 `LOCK_VERIFY_DELAY_MS = 50` 调用 `tryAcquireLock`，换成假时钟后该延时永不触发 → 6 个走刷新路径的测试文件中，`refresh.test.ts` 有 4 例直接超时挂死。逐处改测试驱动时钟不现实，故在 `test/runtime.ts` 引入 `AUTO_FIRE_MAX_DELAY_MS = 100`：

- **短延时（≤100ms）= sleep / yield 语义**（锁校验 50ms、PoW 让出 0ms），在**真实时钟**上自动触发，代码行为与生产一致；
- **长延时 = 调度语义**（重试退避 1s/4s/10s、后台刷新、自走时钟 30s）仍只登记不触发，由用例通过 `activeTimeouts()` / `runTimeout()` 手动驱动——`scheduling.test.ts`、`request-retry.test.ts`、`runtime.test.ts` 的既有断言因此完全不受影响。

顺带修掉一个真实缺陷：`pageFetchJson` 的 20s 超时守卫在 `Promise.race` 结束后**从不清理**，会悬挂到页面生命周期结束；改为 `try/finally` + `runtime.clearTimeout`。

## 5. 验收

1. `export PATH="/Users/hj/.bun/bin:$PATH" && bun run check`（typecheck + lint + format + tests + build）
2. 定向：`bun test test/prism/shared/request-retry.test.ts test/prism/shared/request.test.ts`，以及 `bun test test/prism/sources/v2ex test/prism/weather`
3. dist 核查：`grep -c "BACKOFF_DELAYS_MS" dist/*.user.js` 不应因本改动变化（该常量仍服务于 `app/refresh.ts`）

> 注：prod 产物经 SWC 内联，常量名不可见，实际核查在 `dist/prism.debug.js` 上进行。

## 6. 实施记录

- **S12.1**：`shared/request.ts` 新增 `HTTP_RETRY_DELAYS_MS = [1_000, 4_000, 10_000]`，`computeRetryDelayMs` 回落终点从 `BACKOFF_DELAYS_MS` 切过来；`Retry-After` → `baseDelayMs` → 新阶梯的优先级不变。
- **S12.2**：新增共享常量 `RETRY_ON_429 = { statuses: [429], max: 1 }`，接入 `misc/openrouter/fetcher.ts:30`、`weather/api.ts:58`、`weather/air-quality.ts:34`、`v2ex/fetcher.ts:79`；`xueqiu/fetcher.ts:77` 保持 `max: 2`。
- **S12.3**：按计划**不引入**总预算字段，改用 `max: 1` 约束最坏耗时（openrouter timeout 20s → 最坏 ~41s）。
- **S12.4**：`request-retry.test.ts` 断言改新阶梯并补 3 例（requestJson 429→200、requestText 重试耗尽 reject、无 `Retry-After` 时退避为秒级）；新增 `test/prism/misc/openrouter.test.ts` 2 例端到端验证接线（429→200 恢复 / 重试耗尽 reject）。
- **§4.2 残余（并入）**：5 处裸定时器全部收敛到 Runtime —— `lock.ts:52`、`xueqiu/fetcher.ts:71`（waitJitter）、`:95`（pageFetchJson 守卫）、`card/primitives.tsx:43-44`、`xueqiu/component.tsx`（timerRef 类型收紧为 `number`）。配套：`test/runtime.ts` 增加 `AUTO_FIRE_MAX_DELAY_MS = 100`；`lock.test.ts` 原 8 处调用加 `verifyDelayMs: 0` 并新增 1 例断言延时注册在 Runtime 上。
- **顺带修复**：`pageFetchJson` 的 20s 超时守卫在 `Promise.race` 后从不清理，改为 `try/finally` + `runtime.clearTimeout`。
- **按计划未动**：`reddit/fetcher.ts` 保留自有重试（403→`advanceHost()` 换 host 语义无法用「重试同一 URL」表达）；`xueqiu/ai/summarize.ts`（POST 非幂等）不开重试；cma/safeline、tnews、hupu、novels 不开（各有自有协议或 fallback）。

### 验收结果

- `bun run typecheck` ✅ ／ `bun run lint` 0 error、27 warning（与 S11 基线一致）／ `bun run format` ✅ ／ `bun run build` ✅（prism prod 221.7 KB，hash `f9e5e968`，S11 时为 `a0b7efec`）。
- 测试：`bun test` 合并跑 `test/prism/sources` 仍会 SIGTERM(137)（沙箱资源限制，S11 已知非代码失败），改为按目录分批跑，各批次累计 1300+ 用例 **0 fail**。
- dist 核查（`dist/prism.debug.js`）：`BACKOFF_DELAYS_MS` 仍在（服务 `app/refresh.ts:12642`）、`HTTP_RETRY_DELAYS_MS = [1000,4000,1e4]` 已入包（:1762）、prod 内 `console.debug` 计数 0（S10 规则 intact）。
