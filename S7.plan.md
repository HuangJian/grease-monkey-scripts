# S7 — P1 正确性收尾（剩余项）

> 起点：用户选定"安全/正确性 P0+P1"作为 S6 后的下一步。
> 但复核发现该选项列出的四项（§1.1 / §2.1 / §2.2 主体 / §2.3）已在 S1/S2
> （commit `e159960` "harden sanitizer, escape economy, config validation, break
> import cycles"）落地。本阶段只做**真正还剩**的正确性项，避免重做已完成工作。
>
> 已确认完成（证据）：
>
> - §1.1 xueqiu sanitizer：`xueqiu/component.tsx:6` 引 `shared/sanitize`，`:347/:360` 用 `sanitizeHtml(item.text, …)` 且不再 `unescapeHtml`；黑名单已删。
> - §2.1 `loadConfig` 调 `validateConfig`：`config/load.ts:12`（无效回落 DEFAULT_CONFIG）。
> - §2.2 主体：`keys.ts` 已被 `types/save-filter/export-import/xit` 导入（grep 命中 6 文件）。
> - §2.3 `validate.ts` 含 `rejectUnknownKeys` + `cmaStationId`(`:122`) + novels books/urls(`:215`)。
> - §2.5 `deepMerge` 已 `T extends Record<string,unknown>` + 类型不符 warn+skip（`merge.ts:29`）。
> - §2.8 xueqiu `fetch` 已派生 `rankHotPosts` + `SkipRefreshError`（`xueqiu/source.tsx:172`）。
> - §2.6 主体：11 处双层断言收敛为单点 `toErasure`（`source-registry.ts:19`）。

---

## ① §2.9 配置快照：TTL 改了必须刷新页面才生效

**现状**（构造期数字快照，编辑器改 TTL/阈值不生效）：

- `src/prism/xueqiu/source.tsx:153` `ttlMs: options.ttlMinutes * 60_000`
- `src/prism/novels/source.tsx:20` `ttlMs: options.ttlMinutes * 60_000`
- `src/prism/weather/source.tsx:59` `ttlMs: options.ttlMinutes * 60_000`

**对照（已正确）**：`misc/source.tsx:46-48` 用 `get ttlMs()` getter 规避快照；`reddit/source.tsx:104`、`v2ex/editor.tsx:45-50` 已有 `loadFreshOptions` 但 v2ex source 未调用。

**方案**：

- 三处 `ttlMs` 改为 `get ttlMs() { return this.options.ttlMinutes * 60_000 }`，照抄 `misc/source.tsx:46-48`。
- 若某 source 的 `ttlMinutes` 来自 `options` 且 `options` 是构造期快照对象，则改为每次 `fetch` 开头 `loadConfigSection` 重读（与 `loadFresh*` 一致）；需逐文件确认 `options` 来源（v2ex 的 `loadFreshOptions` 已就绪，直接接）。
- **风险**：中。改 getter 后需确认 `this` 绑定（箭头函数/`bind` 历史写法），并确认 `refreshSource` 读 `source.ttlMs` 的路径（应在 fetch 时求值，已正确）。
- **验证**：加测试——构造 source 后用 `runtime.getValue` 改写 `ttlMinutes`，再次 `fetch` 应看到新 TTL 生效（无需重建 source）。

## ② §2.2 `gm:*` 命名空间残留收敛到 keys.ts

**现状**（裸字符串，未走 keys.ts）：

- `src/prism/misc/source.tsx:8` `const CACHE_KEY_OPENROUTER = 'gm:misc:openrouter:cache'`
- `src/prism/xueqiu/ai/summarize.ts:5` `const SUMMARIES_KEY = 'gm:xueqiu:ai-summaries'`
- `src/shared/author-labels.ts:6` `reddit_author_tags` 与 `:8` `gm:reddit:author-tags` 两个键并存；
  `src/reddit-time-saver/app/index.ts:18` `STORAGE_KEY = 'reddit_author_tags'`（旧键仍被另一入口写）。

**方案**：

- `keys.ts` 增 `OPENROUTER_CACHE_KEY`、`XUEQIU_SUMMARIES_KEY`，两处改为 import。
- `reddit_author_tags` 不一致：统一为 `gm:reddit:author-tags`（keys.ts `REDDIT_AUTHOR_TAGS_KEY`），
  旧键 `reddit_author_tags` 加一次性 `migrateOldKey`（仿 `xit/filters.ts:9`）读旧写新后删除旧键，
  避免已存作者标签丢失。**需确认是否有意保留双键**；若无意，统一 + 迁移。
- **风险**：低（openrouter/xueqiu）→ 中（reddit 标签需迁移旧键）。
- **验证**：grep 全仓不再有裸 `'gm:misc:openrouter:cache'` / `'gm:xueqiu:ai-summaries'` / `'reddit_author_tags'`；reddit 标签迁移单测。

## ③ §2.6 收尾（可选，中风险）：判别联合 `AnySource`

**现状**：`toErasure` 单点双层断言已在（`source-registry.ts:19-21`），但下游仍丢类型——
`card/card.tsx:46` `as T | null`、`card/tabs-card.tsx:37` `as unknown`。
**方案（本阶段默认不做，留给独立阶段）**：定义 `type AnySource = Source<V2exTopic[]> | Source<WeatherData> | Source<NovelData> | …`，
`sources: AnySource[]`，消费处用 `source.id` 收窄；或至少 `assertSourceDataShape<T>()` 编译期断言替代 `as unknown as`。
**理由**：波及 card/render/refresh 三层，且 §2.6 主要风险（11 处散落断言）已消除，收益递减，单独成阶段更稳。

---

## 不做（记录现状）

- §2.4 codec：迁移模块 `codec-migrate.ts` 已存在（`cache.ts:16` 调 `migrateCache`）；仍是
  `schemaVersion !== CACHE_SCHEMA_VERSION` 即丢弃。跨版本迁移仅在升 v3 且字段形状重叠时有意义，留待那时。
- §1.2 escapeHtml 二次转义：S1 "escape economy" 已处理；本阶段仅补一个回归测试锁定（若 `test/` 中尚未覆盖 JSX 文本子节点 escapeHtml 误用）。

## 终态门禁

- `bun run check`（lint 0 / typecheck 0 / format 0 / test 全绿 / build 出 hash）。
- 报告 Build hash；本阶段不增删 dist 孤儿。
- §2.9 与 §2.2 各补纯逻辑单测（构造期快照失效 / 旧键迁移）。
