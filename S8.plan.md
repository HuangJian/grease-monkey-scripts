# S8 — §2.6 `source-registry` 判别联合 `AnySource`（抹除泛型的真正修复）

> 路线图位置：S1–S7 已收官。§2.6 在 S7.plan.md 中被显式**推迟到独立阶段**（"AnySource 判别联合"），理由是其改动面远超 §2.9/§2.2，需独立设计评审。本文件即该阶段的方案。

## 0. 当前状态（重要事实，避免重复劳动）

- **S1/S2（`e159960`）已经落地了 refactor 文档 §2.6 的"最小退路"**：把 11 处散落 `as unknown as Source<unknown>` 收敛为 `source-registry.ts:19` 单点 `toErasure<T>(source: Source<T>): Source<unknown>`。所以"单次断言"目标已达成。
- **本阶段要做的是文档 §2.6 的"主方案"**：定义判别联合 `AnySource`，让注册表持有**带具体 `T` 的源**，消费处用 `source.id` 收窄，从而把泛型信息从"构造期彻底抹掉"升级为"在 `id` 收窄处可恢复"。
- 判别字段：当前 `Source<T>` 的 `id: string` 不是字面量，无法做判别收窄。所以本方案的核心是给 `Source<T>` 增加一个字面量判别参数 `Id`。

## 1. 目标设计

### 1.1 `Source<T, Id>` 增加判别参数

`src/prism/types.ts` 的 `Source<T>`（当前 line 154）改为：

```ts
export type Source<T, Id extends string = string> = {
  readonly id: Id
  readonly title: string
  readonly ttlMs: number | (() => number)
  // ...其余字段保持 T 不变...
}
```

- `Id extends string = string`：默认仍是 `string`，所以**所有现存 `Source<X>` / `Source<unknown>` 用法零改动**（`Source<X>` ≡ `Source<X, string>`）。
- `id` 仍是 `readonly`，字面量来自工厂返回注解。

### 1.2 `AnySource` 判别联合（`types.ts` 新增）

```ts
import type { V2exTopic } from './v2ex/types'
import type { WeatherData } from './weather/types'
import type { NovelData } from './novels/types'
import type { RedditRenderData } from './reddit/types'
import type { HupuRenderData } from './hupu/types'
import type { TnewsItem } from './tnews/types'
import type { XueqiuRenderData } from './xueqiu/types'
import type { XitData } from './xit/types'
import type { MiscData } from './misc/types'

export type AnySource =
  | Source<V2exTopic[], 'v2ex'>
  | Source<WeatherData, 'weather'>
  | Source<NovelData, 'novels'>
  | Source<RedditRenderData, 'reddit'>
  | Source<HupuRenderData, 'hupu'>
  | Source<TnewsItem[], 'tnews'>
  | Source<XueqiuRenderData, 'xueqiu-news'>
  | Source<XueqiuRenderData, 'xueqiu-hot'>
  | Source<XitData, 'xit'>
  | Source<MiscData, 'misc'>
```

成员 `T` 与 `id` 取自各工厂（已逐一核对源码）：
| 工厂 | 返回注解（当前） | 字面 id |
|---|---|---|
| createV2exSource | `Source<V2exTopic[]>` | `'v2ex'` |
| createWeatherSource | `Source<WeatherData>` + 局部 `const source: Source<WeatherData>` | `'weather'` |
| createNovelsSource | `Source<NovelData>` | `'novels'` |
| createRedditSource | `Source<RedditRenderData>` | `'reddit'` |
| createHupuSource | `Source<HupuRenderData>` | `'hupu'` |
| createTnewsSource（`.source`） | `TnewsHandle.source: Source<TnewsItem[]>` | `'tnews'` |
| createXueqiuSources（`.mainSource`/`.hotSource`） | `Source<XueqiuRenderData>` ×2 | `'xueqiu-news'`/`'xueqiu-hot'` |
| createXitSource | `Source<XitData>` + 局部 `const source` | `'xit'` |
| createMiscSource | `Source<MiscData>` | `'misc'` |

### 1.3 各工厂加字面量注解（关键：让 `id` 成为字面量而非 `string`）

TS 默认把对象字面量 `id: 'v2ex'` 推断为 `string`；要让 `Source<T, Id>` 的 `Id` 收窄为 `'v2ex'`，**必须**在返回类型（及局部 `const source` 绑定）显式写出字面量：

- `v2ex/source.tsx:19` → `(): Source<V2exTopic[], 'v2ex'>`
- `weather/source.tsx:58` 函数返回 + `:62` 局部 `const source: Source<WeatherData>` → 均加 `, 'weather'`
- `novels/source.tsx:16` 函数返回 → `Source<NovelData, 'novels'>`
- `reddit/source.tsx:23` 函数返回 → `Source<RedditRenderData, 'reddit'>`
- `hupu/source.tsx:23` 函数返回 → `Source<HupuRenderData, 'hupu'>`
- `tnews/source.tsx:14` `TnewsHandle.source` → `Source<TnewsItem[], 'tnews'>`
- `xueqiu/source.tsx:31/32` `XueqiuHandle.mainSource/hotSource` + `:58/:150` 局部 `const` → `Source<XueqiuRenderData, 'xueqiu-news'/'xueqiu-hot'>`
- `xit/source.tsx` 函数返回 + 局部 `const source: Source<XitData>`（:27）→ 加 `, 'xit'`
- `misc/source.tsx:34` 函数返回 → `Source<MiscData, 'misc'>`

> 局部 `const source: Source<X> = {...}` 若不加分型，返回 `Source<X, string>` 将与 `AnySource` 成员（`Id` 字面量）不兼容 → 注册表赋值报错。这是本阶段最容易踩的坑，已逐一列出。

### 1.4 注册表 / 查找（去掉 `toErasure`）

`src/prism/app/source-registry.ts`：

- `sources: AnySource[] = [ createV2exSource(...), createWeatherSource(...), ... ]` —— 每个元素是 `AnySource` 的一个成员，直接可赋值，**删除 `toErasure` 函数及全部 11 处调用**（当前 line 19–21、27–39）。这是本方案对文档"11 处双层断言"的真正清零。
- `findSource(sources: AnySource[], id: string): AnySource | undefined`（当前 line 52）→ 返回类型从 `Source<unknown>` 升级为 `AnySource`。消费处 `if (s && s.id === 'v2ex')` 即可收窄到 `Source<V2exTopic[], 'v2ex'>`。

### 1.5 `CardGroup` 透传联合

`src/prism/card-group.ts`：`CardGroup.tabs: AnySource[]`、`buildCardGroups(sources: AnySource[], ...)`、局部 `singletons: AnySource[]`。`.forEach((source) => ...)` 仅读 `groupId/placement/id/order`，对 `AnySource` 安全，无需断言。

### 1.6 刷新边界的残余擦除（诚实标注）

`refresh.ts` 内部对 `source` 做异质调用（`source.fetch(runtime, prevData)` 其中 `prevData` 来自缓存为 `unknown`）。判别联合下 `source.fetch` 是"按成员不同的函数联合"，无法直接统一调用，必须在**收窄或断言**后才能调。最小改动：保持 `refresh.ts` 全程 `Source<unknown>` 不变，在 `app/index.ts` 的两个入口把 `AnySource` 断言回 `Source<unknown>`（单行、注释说明）：

- `app/index.ts:105` `const source = findSource(reg.sources, sourceId) as Source<unknown> | undefined`
- `app/index.ts:114` `runOpportunisticRefresh(runtime, reg.sources as Source<unknown>[], (s) => refreshAndRerender(s as Source<unknown>))`

→ 擦除点从"注册表 11 处"收敛为"刷新边界 2 处（且 `as Source<unknown>` 单断言，非 `as unknown as` 双断言）"。

### 1.7 渲染边界的残余擦除（固有，不可消除）

`tabs-card.tsx:37` / `card.tsx:46` 的 `as unknown` / `as T | null` 来自**异构 Tab 数组**把 `data` 当 `unknown` 喂给 `RenderComponent`。这是"一个 `CardGroup` 容纳多种 `T`"的固有擦除，判别联合本身无法消除（循环不收窄）。

为把它收敛为**单一有名字的擦除点**，新增 helper（可选但推荐）：

```ts
// types.ts 或 card 内
export function readSourceData<T, Id extends string>(
  _source: Source<T, Id>,
  cached: CachedSource<unknown> | null,
): T | null {
  return (cached?.data ?? null) as T | null
}
```

- `card.tsx:46` → `const data = readSourceData(source, cached)`（`source: Source<T>` → 真实 `T` 恢复，无需 `as`）。
- `tabs-card.tsx:37` → `const activeData = readSourceData(activeTab, activeCached)`（此处 `activeTab: AnySource` → `T` 为联合，`as T | null` 等价于 `as unknown`，仅作单一有名字点；真正受益在 `findSource` 收窄后的调用）。

## 2. 改动文件清单

| 文件                                                      | 改动                                                                               |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `src/prism/types.ts`                                      | `Source<T, Id>`；新增 `AnySource`；新增数据类 import；可选 `readSourceData`        |
| `src/prism/v2ex/source.tsx`                               | 返回注解加 `'v2ex'`                                                                |
| `src/prism/weather/source.tsx`                            | 函数返回 + 局部 `const source` 加 `'weather'`                                      |
| `src/prism/novels/source.tsx`                             | 返回注解加 `'novels'`                                                              |
| `src/prism/reddit/source.tsx`                             | 返回注解加 `'reddit'`                                                              |
| `src/prism/hupu/source.tsx`                               | 返回注解加 `'hupu'`                                                                |
| `src/prism/tnews/source.tsx`                              | `TnewsHandle.source` 加 `'tnews'`                                                  |
| `src/prism/xueqiu/source.tsx`                             | `XueqiuHandle.main/hot` + 局部 `const` 加 `'xueqiu-news'/'xueqiu-hot'`             |
| `src/prism/xit/source.tsx`                                | 函数返回 + 局部 `const source` 加 `'xit'`                                          |
| `src/prism/misc/source.tsx`                               | 返回注解加 `'misc'`                                                                |
| `src/prism/app/source-registry.ts`                        | 删 `toErasure`；`sources: AnySource[]`；`findSource` 返回 `AnySource`              |
| `src/prism/card-group.ts`                                 | `tabs`/`sources` 参数改为 `AnySource[]`                                            |
| `src/prism/app/index.ts`                                  | 刷新入口 2 处 `AnySource → Source<unknown>` 断言                                   |
| `src/prism/card/card.tsx`、`src/prism/card/tabs-card.tsx` | 用 `readSourceData` 替换散落 `as`                                                  |
| `test/prism/app/any-source.test.ts`（新增）               | 回归：`findSource` 按 id 返回正确成员；`source-registry.ts` 不再含 `as unknown as` |

> 不改动：`refresh.ts`、`cache.ts`、`codec*.ts`、`group-renderer.ts`、`export-import.ts`（其 `Source<XitData>` 为 Id=string 默认，兼容）、`browse-state.ts`/`save-filter.ts`/`shell/editor.tsx`/`sync-bootstrap.ts` 的 `Source<unknown>` 用法（Id=string 默认，兼容）。

## 3. 诚实的残留擦除点（评审要点）

完成本方案后，泛型擦除仅剩两处、均为单断言、且有明确命名/注释：

1. **刷新边界**（`app/index.ts` 2 处 `as Source<unknown>`）：因 `refresh.ts` 对 `source.fetch(prevData: unknown)` 做异质调用，判别联合无法直接统一调用。要彻底消除需把 `refresh.ts` 也改成按 `id` 收窄的判别分发——那是更大的独立重构，超出 §2.6，建议留待 §4 系列。
2. **渲染边界**（`readSourceData` 在异构 Tab 循环里 `T` 退化为 `unknown`）：一个 `CardGroup` 容纳多种 `T` 的固有性质。要彻底消除需把 `CardGroup`/`CardTab` 也做成判别联合并让 `RenderCard` 按 `id` 分发——同样是更大的独立重构。

即：本方案交付文档 §2.6 明确要求的 `AnySource` 判别联合 + 注册表零 `as unknown as` + 类型化 `findSource`；文档提到的"下游 `card.tsx:46`/`tabs-card.tsx:37` 不再补断言"**只能部分达成**（收敛为单一有名字 helper，但异构循环内 `T` 仍为 `unknown`）。这一点如实标注，不做过度承诺。

## 4. 验证

- `bun run check`（lint / typecheck / format / test / build）全绿为门槛。
- 新增 `test/prism/app/any-source.test.ts`：
  - 构造 `createSourceRegistry` → `findSource(reg.sources, 'weather')?.id === 'weather'`、`findSource(reg.sources, 'xueqiu-news')?.id === 'xueqiu-news'`。
  - 一个**编译期**收窄示例（放在被测函数里，靠 `bun run check` 的 typecheck 把关）：`const s = findSource(reg.sources, 'v2ex'); if (s && s.id === 'v2ex') { const _t: V2exTopic[] | null = s.RenderComponent.props extends ... }` —— 简化版：断言 `s.fetch` 返回 `Promise<V2exTopic[]>`（用 `expectTypeOf` 若可用，否则仅编译通过即可）。
- 加一条 lint 友好的静态断言：在测试里 `import { readFileSync }` 读 `source-registry.ts` 源码、`expect(content).not.toContain('as unknown as')`，锁定"注册表零双断言"不回归。

## 5. 风险 / 回退

- **字面量注解遗漏**（1.3 表）：任一工厂漏加分型 → `Source<X, string>` 不兼容 `AnySource` 成员 → 注册表赋值 typecheck 失败。已逐文件列出，实施时逐项核对。
- **`Source<T, Id>` 默认 `string` 保证向后兼容**：所有未改的 `Source<X>` 用法不受影响；若 `bun run check` 仍报其他 `Source<unknown>` 不兼容，多半是该处本就期望字面量——按报错点补注解即可。
- 回退简单：`git revert` 单 commit 即回到 S7 状态（注册表 `toErasure` 单点擦除）。
