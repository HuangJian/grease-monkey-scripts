# S10 — §5 工程化收尾（prod console.debug 剥离 + oxlint 规则面）

> 设计先行：本文件待 review，实施前不改代码。S9 已提交 `d2e0f22`（§4 架构收敛）。S6 已覆盖 §5.1/§5.2/§5.5。本阶段补 §5 剩余两项 **§5.3** 与 **§5.4**。

## 1. 背景与目标

- **§5.3** — prod `.user.js` 在 SWC minify **之后**仍被追加 1 条 `console.debug('name:build <hash>')`，违反 AGENTS.md:183「Build strips `console.log`/`console.debug` from `.user.js`」。debug `.debug.js`（同源追加）按约定应**保留** `console.debug`（AGENTS.md:183「keeps them in `.debug.js`」），故不改。
- **§5.4** — `.oxlintrc.json` 仅开 `correctness`/`suspicious` + `import/no-cycle`（S2 已加，见 `.oxlintrc.json:19`）。文档建议「至少补 `import/no-cycle`，可选补 `perf`」。必做部分已完成，本阶段仅补可选 `perf`。

## 2. 现状核对（已查证，非假设）

- `scripts/build-userscript.ts:407`（debug）与 `:427`（prod）均写 `console.debug('${name}:build ${hash}')`。
- `parseBuildHash`（`scripts/build-utils.ts:170-174`）当前正则 `/^.+?build ([a-f0-9]+)'\)\s*$/` 仅解析 `console.debug` 形式；`readExistingHash` 注释（`:212`）写「Expects `// build <hash>`」与实现**不符**（实现读 console.debug）—— stale 注释，功能正常（写/读同为 console.debug，hash-skip 生效）。
- hash 仅由 `bundle + buildScriptSource` 计算（`build-userscript.ts:402`），marker 在写文件时追加、**不进 hash** → 改 marker 文本不触发重建；但 `buildScriptSource` 含 `build-userscript.ts`+`build-utils.ts`（`:387-390`），改源会触发**一次全量重建**（预期内，dist 不提交）。
- 测试 `test/build-utils.test.ts:218-240` 覆盖 `console.debug` 形式 + 若干 null 用例。
- `import/no-cycle: "error"` 已在 `.oxlintrc.json:19`（S2 加）→ §5.4 必做项已完成。

## 3. Goals

1. **§5.3**：prod `.user.js` 不再含 `console.debug`；尾行标记改 `// <name>:build <hash>` 注释。debug `.debug.js` 保持 `console.debug`（守 AGENTS.md:183）。`parseBuildHash` 兼容注释新格式 **与** 旧 `console.debug` 格式（过渡期旧 dist 仍可正确跳过重建）。
2. **§5.4**：`.oxlintrc.json` 启用 `"perf": "warn"`（非阻塞警告）。**不启用** `style`（oxfmt 已管格式化，style 规则冗余/冲突，收益<风险）。

## 4. Non-goals

- 不改 §5.1/§5.2/§5.5（S6 已做）。
- 不补 §5.6 测试覆盖缺口（留独立阶段）。
- 不启用 `style` 类 lint（与 oxfmt 重叠）。
- 不改 debug `.debug.js` 的 `console.debug`（AGENTS.md:183 允许保留）。
- 不改 hash 计算逻辑。

## 5. 关键事实 / 设计要点

- **marker 不参与 hash**：`:402` 的 `computeHash(bundle + buildScriptSource)` 不含尾行 marker → 纯文本替换安全；`readExistingHash` 跳过重建依赖 `parseBuildHash` 仍解析旧 `console.debug` 行。
- **新正则需双格式兼容**：prod 旧文件为 `console.debug(...)`、新文件为 `// ...`，故用 `/build ([a-f0-9]{8})/`（HASH 恒为 8 hex）。逐条核对 `test/build-utils.test.ts:220-240`：
  - `220-221/225-226` console.debug → `abcdef12` ✅
  - `230` `// some code` 无 build → null ✅
  - `234` `// build xyz`（非 8 hex）→ null ✅
  - `238` console.debug 尾随空格 → `aabbccdd` ✅
  - 新增注释格式 `// reddit-time-saver:build abcdef12` → `abcdef12` ✅（待补测试）

## 6. 实施步骤

1. `scripts/build-userscript.ts:427` — 尾行由 `console.debug('${name}:build ${hash}')` 改为 `// ${name}:build ${hash}`。**保持 `:407` 不变**（debug 保留 console.debug）。
2. `scripts/build-utils.ts:170-174` — `parseBuildHash` 正则改为 `/build ([a-f0-9]{8})/`；修正两处 stale 注释：
   - `:168`「Expects ... `console.debug'<name>:build <hash>'`」→「Expects last line `// <name>:build <hash>` (also accepts legacy `console.debug` form for old dist)」。
   - `build-userscript.ts:212` 同改。
3. `.oxlintrc.json` — `categories` 增 `"perf": "warn"`；保留 `import/no-cycle: "error"`；不加 `style`。
4. `test/build-utils.test.ts` — 增 1 例注释格式 `// reddit-time-saver:build abcdef12` → `'abcdef12'`；可选增 1 例验证新旧并存解析。

## 7. 验证

- `bun run typecheck` / `bun run format` → 0。
- `bun run lint` → `perf: "warn"` 仅产生警告，**不使 lint 失败**；报告 perf 警告条数。若发现属 real bug 的 perf 警告，修复或保留 warn（不升级为 error）。
- `bun run build`（因源变更触发全量重建）→ `grep -rn "console.debug" dist/*.user.js` 应为空；`dist/*.user.js` 尾行含 `// <name>:build <hash>`；`dist/*.debug.js` 尾行仍含 `console.debug`。
- `bun run test` → `build-utils` 测试通过（含新增注释用例）；整体测试数 ≥ 改前。
- 报告 Build hash（本次改 `build-userscript.ts`/`build-utils.ts` 源 → 下次 build 全量重建；dist 不提交，由 build 再生）。

## 8. 风险

- **低**。**§5.3** 纯尾行文本替换，不动 hash/逻辑；`parseBuildHash` 正则已核对现有测试全过，且兼容旧 dist 格式（过渡安全）。
- **§5.4** `perf: "warn"` 仅警告，不破坏 lint 门禁；若 warn 量级过大可不开（但文档建议补，默认开）。
- 全量重建为预期内副作用（dist 不入库）。
