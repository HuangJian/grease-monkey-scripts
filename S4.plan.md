# S4 — Runtime 收敛定时器与时钟（Plan）

> 续 `frontend.refactor.md` §4.2（P3）。前置：S1–S3 已提交（S3 = `730cdda`，含 `prune.ts` / `date-parse.ts` / `concurrency.ts`）。
> 本计划**只设计、不落地代码**，待审批后实施。

## 1. 目标

把"宿主定时器 + 系统时钟"收敛进 `Runtime` 抽象，使：

1. `Runtime` 暴露 `now()` + `setTimeout/clearTimeout/setInterval/clearInterval`，成为唯一宿主边界。
2. 浏览器实现（`createBrowserRuntime`）委托宿主全局定时器；测试桩（`createRuntime`）提供可控假时钟 + 定时器注册表，支持确定性测试。
3. 替换所有"裸定时器"调用，删除 `test/prism/scheduling.test.ts` 对 `globalThis.setTimeout/...` 的 monkey-patch（该 patch 污染全局、并行测试不安全——§4.2 已指出）。

## 2. 非目标（明确排除）

- **`src/shared/tag-panel/behavior.ts:25`** 与 **`src/hanzi-dictionary/app/popup.ts:118`** 的 `setTimeout`：**不动**。二者属于独立 app，不经过 prism `Runtime`（tag-panel 跨 app 共享、hanzi-dictionary 自成一体的 userscript）。若日后要收敛，单独立项，不在 S4 范围。
- **全量迁移 67 处 `Date.now()`**：不做（见 §3.4 的范围决策）。
- **`src/runtime.ts:117`**：`requestIdleCallback` 内部的 `setTimeout` 回退是 Runtime 实现自身对宿主的调用，保留全局 `setTimeout`（无法自引用 `runtime.setTimeout`），不计入迁移。

## 3. 设计决策

### 3.1 `Runtime` 类型新增（锚点 `src/runtime.ts:26-54`）

```ts
export type Runtime = {
  // …现有字段不变…
  /** 当前时间（ms）。测试可经 TestRuntime 假时钟覆盖。 */
  now(): number
  /** 返回 number 句柄（与浏览器 setTimeout 返回值类型一致）。 */
  setTimeout(cb: () => void, delayMs?: number): number
  clearTimeout(id: number): void
  setInterval(cb: () => void, delayMs?: number): number
  clearInterval(id: number): void
}
```

- `createBrowserRuntime()`（`src/runtime.ts:91-138`）新增：
  - `now: () => Date.now()`
  - `setTimeout: (cb, ms) => setTimeout(cb, ms) as unknown as number`（浏览器侧返回 number，安全 cast）
  - `clearTimeout` / `setInterval` / `clearInterval` 同理。
- 句柄统一用 `number`：浏览器返回 number；测试桩自管 id 也返回 number。`app/index.ts` 现有 `ReturnType<typeof setInterval>` 类型字段改为 `number | null`（见 §4.1）。

### 3.2 测试假时钟（`test/runtime.ts` 的 `TestRuntime` / `createRuntime`）

仅给 `TestRuntime` 增补（不污染 `Runtime` 契约）：

```ts
// 计时器注册表
type FakeTimer = { id: number; delay: number; cb: () => void; type: 'timeout' | 'interval' }
// TestRuntime 新增字段/方法：
now(): number                                  // 默认 Date.now()，setClock 后返回 clock
setTimeout(cb, delay = 0): number              // 入注册表，返回 id
clearTimeout(id): void                         // 标记 cleared
setInterval(cb, delay = 0): number
clearInterval(id): void
// —— 测试辅助（沿用 scheduling.test.ts 既有断言语义）——
setClock(ms: number): void                     // 覆盖 now()
activeTimeouts(minDelay?: number): FakeTimer[]   // 未 cleared/fired 且 delay>=minDelay
activeIntervals(): FakeTimer[]
runTimer(id: number): void                     // 触发单次回调（interval 重排）
```

**关键默认**：`now()` 默认 `Date.now()`（非 0）。这样既有测试（未调 `setClock`）行为不变；需确定性的测试显式 `setClock(N)`。避免把全仓既有测试默默改成"时钟=0"而破坏相对时间/裁剪语义。

### 3.3 裸定时器迁移清单（实测 15 处 / 6 文件，非文档所言"8 处"）

> ⚠️ 对 §4.2 的纠正：`frontend.refactor.md:244` 列 "8 处裸定时器" 严重少算。实测（grep `(setTimeout|setInterval|clearTimeout|clearInterval)\s*\(` 于 `src`，排除 runtime.ts 实现自身）prism 范围内 **15 处调用 / 6 文件**。文档把 `app/index.ts:44,142` 当成 2 处、xueqiu 两文件拆成 4 处，但漏了 `app/index.ts:42,56,151` 三处 clear 与 `card/primitives.tsx:42` 的 `clearInterval`，也未区分 `lock.ts` 实际只有 1 处。以下为权威清单。

| 文件                             | 行                     | 当前                                                                                                | 改为                                                                                     | runtime 是否已在作用域                                                                                               |
| -------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/prism/app/index.ts`         | 36,37,42,44,56,142,151 | `let …: ReturnType<typeof setInterval>` / `setTimeout`/`clearTimeout`/`setInterval`/`clearInterval` | `runtime.*`；字段类型改 `number \| null`                                                 | ✅ `createDashboard(runtime, …)`                                                                                     |
| `src/prism/reddit/fetcher.ts`    | 57                     | `setTimeout(() => attempt(), …)`                                                                    | `runtime.setTimeout(() => attempt(), …)`                                                 | ✅ `fetchOneSub(runtime, …)` 闭包                                                                                    |
| `src/prism/xueqiu/fetcher.ts`    | 66                     | `waitJitter` 内 `setTimeout(resolve, ms)`                                                           | 给 `waitJitter` 增 `runtime` 参数 → `runtime.setTimeout`                                 | ⚠️ 需给 `waitJitter(runtime, …)` 加参（调用点 `:194` 所在函数已有 `runtime`）                                        |
| `src/prism/xueqiu/fetcher.ts`    | 100                    | `pageFetchJson` 内 `setTimeout(...)`                                                                | `runtime.setTimeout(...)`                                                                | ✅ `pageFetchJson(runtime, …)`                                                                                       |
| `src/prism/xueqiu/component.tsx` | 121,183,184,191        | `clearInterval`/`setInterval`(timerRef)                                                             | `runtime.clearInterval`/`runtime.setInterval`                                            | ✅ 组件闭包已有 `runtime`                                                                                            |
| `src/prism/card/primitives.tsx`  | 41,42                  | `setInterval(() => setTick(Date.now()), 30000)` + `clearInterval(id)`                               | `runtime.setInterval(() => setTick(runtime.now()), 30000)` + `runtime.clearInterval(id)` | ⚠️ 需给 `RefreshTime`+`CardActions` 加 `runtime` prop（调用点 `card.tsx:71,83` / `tabs-card.tsx:81` 均有 `runtime`） |
| `src/prism/lock.ts`              | 52                     | `setTimeout(r, verifyDelayMs)`                                                                      | `runtime.setTimeout(r, verifyDelayMs)`                                                   | ✅ `tryAcquireLock(runtime, …)`                                                                                      |

### 3.4 `Date.now()` 迁移范围（**已拍板：方案 B 全量**）

实测 **67 处 `Date.now()` / 38 文件**（`src/prism`）。用户已选 **B：全量迁移**。执行规则（按"runtime 可达性"分层，保证 `bun run check` 绿且不破坏语义）：

1. **runtime 已在词法作用域** → 直接 `Date.now()` 改 `runtime.now()`。覆盖绝大多数 `source.tsx` / `fetcher.ts` / `component.tsx` / `app/*` 站点（含 §3.3 定时器同处的 card/primitives、xueqiu/component，以及 4 个 `pruneExpiredCache` 入口）。
2. **纯函数/无 runtime 作用域**（parser、`shared-utils.ts`、`date-parse.ts`、`item-state.ts`、`item-actions.ts`、`novels/fetcher.ts`、`weather/source.tsx`、`xueqiu/ai/summarize.ts` 等）→ 按 `lock.ts:14,31-32` 既有注入模式，给函数加可选参数 `now: () => number = Date.now`（或并入现有 options 对象 `now?: () => number`）。
   - 默认 `Date.now()`：**既有调用方无需改动**（向后兼容）。
   - 调用方若已有 `runtime`，则显式传 `runtime.now`；无则沿用默认。
   - 这样每处时间都可被 Runtime 覆盖，且零强制 churn。
3. **`now()` 测试默认**：`Date.now()`（用户拍板）。`createRuntime` 的 `now()` 默认返回系统时间，仅 `setClock(N)` 时覆盖。既有测试行为不变。

> 全量迁移后，`frontend.refactor.md` §4.2 的"60+ 处裸 Date.now()"反例消除；纯函数通过 `now` 参数获得注入能力（与 lock.ts 一致）。

### 3.5 `lock.ts` 的 `now` 默认

`lock.ts:31` 已注入 `now?: () => number`（`options.now ?? Date.now`）。改为 `options.now ?? (() => runtime.now())`——把锁的"时间来源"也收到 Runtime，与 §3.1 一致。这是文档 §4.2 点名的"全仓唯一已做时间注入的模块"的正面示范，应当顺势闭环。

## 4. 分步实施

1. **`src/runtime.ts`**：`Runtime` 增 `now/setTimeout/clearTimeout/setInterval/clearInterval`（§3.1）；`createBrowserRuntime` 实现委托宿主全局。
2. **`test/runtime.ts`**：`TestRuntime` 增假时钟 + 注册表 + 辅助（`setClock`/`activeTimeouts`/`activeIntervals`/`runTimer`）；`createRuntime` 实现（默认 `now = Date.now`）；`TestRuntimeBuilder` 可选加 `withClock`。
3. **定时器迁移**（§3.3 表）：逐文件改 `runtime.*`，`card/primitives.tsx` 加 `runtime` prop 并透传 `card.tsx`/`tabs-card.tsx`；`xueqiu/fetcher.ts` 给 `waitJitter` 加参。
4. **`Date.now()` 迁移**（按 §3.4 选定方案）：A 则改 6 处；B 则全量。
5. **`lock.ts`**：`now` 默认改 `() => runtime.now()`（§3.5）。
6. **`test/prism/scheduling.test.ts`**：删除 `installTimerMock` 全局 patch（`:11-70` 及其 `beforeEach/afterEach` 调用）；改读 `runtime.activeTimeouts/activeIntervals/runTimer/setClock`，断言语义不变（delay 值、active 状态、foreground 60s interval 过滤）。
7. **`bun run check`**：typecheck + lint + format + test + build 必须全绿。

## 5. 验证

- `bun run check`（项目统一门禁）。
- 新增 `test/runtime.test.ts`：假时钟 `setClock` 改变 `now()`；`setTimeout` 入注册表、`runTimer` 触发、`clearTimeout` 生效；`setInterval` 重排；与原全局 patch 行为对齐。
- `scheduling.test.ts` 重写后断言保持：background 300–360s、foreground 60s interval、close 恢复 background。

## 6. 决策记录

- **§3.4 范围**：已选 **方案 B（全量迁移）**。runtime 可达处直接 `runtime.now()`；纯函数按 `lock.ts` 注入模式加 `now: () => number = Date.now` 可选参数，默认 Date.now 保向后兼容。
- **`now()` 测试默认**：已选 **`Date.now()`**（推荐）。仅 `setClock(N)` 覆盖。
- 计划已审批，进入实施。

## 7. 完成状态（2026-09-02）

S4 已完整落地，非"只设计"。实际改动与计划偏差在此登记：

### 7.1 已落地

- **`src/runtime.ts`（T1）**：`Runtime` 新增 `now()/setTimeout/clearTimeout/setInterval/clearInterval`；`createBrowserRuntime()` 委托宿主全局定时器（句柄 `as unknown as number`）。与 §3.1 一致。
- **`test/runtime.ts`（T2）**：`TestRuntime` 增假时钟（`setClock` / `now()` 默认 `Date.now`） + 定时器注册表（`timers: Map<number, FakeTimer>`）。对外辅助方法命名：计划写 `setClock/activeTimeouts/activeIntervals/runTimer`，落地为 `setClock/activeTimeouts(minDelay?)/activeIntervals/runTimeout(id)`（`runTimeout` 对 interval 为 no-op，单次触发）。语义与 §3.2 一致。
- **定时器迁移（T3，§3.3 清单 15 处 / 6 文件全改）**：
  - `app/index.ts`：字段类型 `ReturnType<typeof setInterval>` → `number | null`；`scheduleBackgroundRefresh` / `clearBackgroundRefresh` / `open` / `close` 全部改 `runtime.*`。（⚠️ 该文件一度被 checkout 回滚、需重做并 grep 复核 5 处 `runtime.*`。）
  - `reddit/fetcher.ts` / `xueqiu/fetcher.ts`（含 `waitJitter(runtime, …)` 加参 + `pageFetchJson` 超时守卫）/ `xueqiu/component.tsx`（timerRef 改 `runtime.*`）/ `card/primitives.tsx`（`runtime` prop 透传 `card.tsx`+`tabs-card.tsx`）/ `lock.ts`。
- **`Date.now()` 全量迁移（T5，方案 B）**：67 处全部收敛。结果：45 处 `runtime.now()`；11 处 `Date.now()` 合法保留——
  - `runtime.ts:145` 实现委托（自身不能自引用）；
  - `date-parse.ts:35,80` 与 `item-state.ts:65,70` 为规则 B 默认 `Date.now`；
  - `item-state.ts:164,168` 非可观测 `expandedAt`；
  - `sudugu.ts:61` 用户保留默认；
  - `v2ex/parser.ts:52`、`v2ex/fetcher.ts:41,98` 为注释/字符串。
- **`lock.ts`（T4）**：`now` 默认改 `options.now ?? (() => runtime.now())`，闭环 §3.5。
- **测试（T6）**：
  - `scheduling.test.ts`：删除 `installTimerMock` 全局 `globalThis.setTimeout` monkey-patch 及其 `beforeEach/afterEach` 调用；改读假时钟（`activeTimeouts/activeIntervals/runTimeout/setClock`）。断言保持（background 300–360s、foreground 60s interval、close 恢复 background）。
  - `runtime.test.ts`：新增 `describe('TestRuntime fake clock')` 7 测试（`now()` 默认 + `setClock`、`setTimeout` 注册 + `runTimeout` 单次、`clearTimeout`、`setInterval`+`clearInterval`、interval `runTimeout` no-op、不自动触发）。补 `afterEach/beforeEach` 导入。

### 7.2 文档纠正

- **定时器计数**：`frontend.refactor.md:244` 称"8 处裸定时器"严重少算。实测 **15 处 / 6 文件**（见 §3.3）。此差异已在 §3.3 顶部标注。
- **`Date.now()` 计数**：§3.4 计划写"67 处 / 38 文件"（实测 grep 值）；落地全量后剩余 11 处为合法保留（上列），非遗漏。

### 7.3 验证

- `bun run check` 全绿：lint ✓ / typecheck ✓ / format ✓ / test **1435 pass / 0 fail** / build ✓（prism hash `59c44bd9`）。
- 中途一次 `bun run check` 报 1 fail 为 flaky（隔离重跑通过），非回归。
- 改动未提交（按 AGENTS.md 不自动 commit）。待用户 `commit` 推进 S5。
